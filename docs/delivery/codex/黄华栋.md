# 你是一名小组成员，你们小组需要完成一份小组作业，基于https://github.com/open-webui/ope…

> 你是一名小组成员，你们小组需要完成一份小组作业，基于[https://github.com/open-webui/open-webui](https://github.com/open-webui/open-webui) 项目，开发课程专属AI助手，你选择的课程是数据结构，系统应能够根据教师提供或小组整理的课程资料回答问题，帮助学生理解知识点、复习课程内容并完成自测。应主要利用其模型配置、系统提示词、知识库、RAG 和工具扩展能力完成项目。  &#x20;
>
> 现在组内分工如下：
>
> 组长
>
> 1. 项目统筹：进度跟踪、跨成员联调、解决卡点。
> 2. 部署验证：确认 start-dev.bat、后端、前端可跑通，DeepSeek 可用；补一份部署说明（基于现有成果，工作量不大，但要留证据截图/记录）。
> 3. AI 助教：创建「XX 课程 AI 助教」模型/智能体，编写系统提示词，覆盖——身份、课程范围、回答格式、引用要求、学术诚信、无法确认时的处理、作业不代写原则。
> 4. 集成：把知识库、扩展工具、系统提示词挂到助教上，做端到端联调。
> 5. 交付：git 分支与提交规范、仓库链接整理、项目总结报告主笔、汇总 Codex 交互记录。
>    组员 A — 知识库
>
> - 资料收集与整理：课件、实验指导书、教材节选、示例代码与常见错误说明，至少 3 类、按章节/模块建目录、统一格式（建议以 Markdown 为主，切分和检索效果最可控）。
> - 上传知识库，配置 RAG（嵌入模型、chunk size、top\_k、是否 rerank）。
> - 配合第 3 天的“优化前后对比”，负责调整知识库内容/文档切分/检索参数。
>   组员 B — 扩展工具
> - 调研 Open WebUI Workspace Tool / Skill / Functions 的开发方式。
> - 实现一个有真实结构化输入输出的工具，建议做「章节随机抽题 + 客观题自动判分」或「练习题生成」，避免只靠提示词（题目明确要求：仅提示词模拟不计分）。
> - 本地联调、接入助教，写使用说明和测试样例。
>   组员 C — 测试与评价
> - 设计 ≥15 个测试问题，覆盖要求：5 课程知识问答、3 综合分析、2 知识库无答案、2 练习题生成/批改、2 工具调用、1 错误输入/异常。
> - 对每题记录：输入、实际输出、引用是否正确、回答是否正确、问题、改进方式。
> - 组织一次优化前后对比（同批问题，改提示词/切分/检索参数前后各跑一遍，做成表格）。
> - 整理 Codex 交互记录。
>
> 现在需要你担任组员B，列出你的计划一步步进行，完成相应任务

<details><summary>20 previous messages</summary>

> 我先制定计划，然后开始探索环境和调研。
>
> ## 组员B 工作计划（扩展工具）
>
> **第一步：环境与调研**
> - 摸清 `D:\暑期实训\小组作业\open-webui-ai` 仓库结构，找到 Tools/Functions 相关目录
> - 调研 Open WebUI 的 Workspace Tool / Skill / Functions 三种扩展的开发方式与差异
> - 确认本地运行实例（若已部署）及 DeepSeek 配置
>
> **第二步：设计**
> - 确定工具形态（优先 Workspace Tool，因为它是 Open WebUI 原生"工具调用"扩展，符合"真实结构化输入输出"要求）
> - 选定功能：「章节随机抽题 + 客观题自动判分」（有真实的输入 schema、输出 JSON、判分逻辑，而非纯提示词）
>
> **第三步：实现**
> - 编写工具代码（题库数据 + 抽题 + 判分逻辑）
> - 本地单测（脱离 WebUI 直接跑 Python 验证）
>
> **第四步：接入与联调**
> - 将工具放进 Open WebUI 的工具目录，配置挂到助教
> - 通过 API/界面端到端验证
>
> **第五步：文档**
> - 写使用说明、测试样例、交互记录
>
> 下面开始探索环境。
>
> <details><summary>Explored 3 files, ran 58 commands</summary>
>
> - Ran `Get-ChildItem -Path . -Directory -Recurse -Depth 2 | Where-Object { $_.Name -match 'tool|function|skill|pipe|filter' } | Select-Object FullName | Format-Table -AutoSize`
>
> - Ran `Get-ChildItem -Force | Select-Object Mode,Length,Name | Format-Table -AutoSize`
>
> - Ran `Get-ChildItem -Path backend\open_webui\tools -Recurse -Depth 1 | Select-Object Mode,Length,FullName | Format-Table -AutoSize`
>
> - Ran `Get-ChildItem -Path backend\open_webui -Directory | Select-Object Name | Format-Table -AutoSize; Write-Output '--- functions/tools search ---'; Get-ChildItem -Path backend\open_webui -Directory -Recurse -Depth 1 | Where-Object { $_.Name -match 'function|tool|skill|pipe|filter' } | Select-Object FullName | Format-Table -AutoSize`
>
> - Ran `Get-Content backend\open_webui\tools\builtin.py -TotalCount 120`
>
> - Ran `Get-Content backend\open_webui\tools\knowledge_fs.py -TotalCount 80`
>
> - Ran `Select-String -Path backend\open_webui\tools\builtin.py -Pattern 'class Tool|def get_tools|Tool\(|name=|description=|parameters|class Tools' | Select-Object LineNumber,Line | Format-Table -AutoSize -Wrap`
>
> - Ran `Select-String -Path backend\open_webui\**\*.py -Pattern 'builtin|get_tools|Tool' -List | Select-Object Path,LineNumber,Line | Format-Table -AutoSize -Wrap`
>
> - Ran `Get-Content backend\open_webui\models\tools.py -TotalCount 160`
>
> - Ran `Get-Content backend\open_webui\utils\tools.py -TotalCount 200`
>
> - Ran `Get-Content backend\open_webui\utils\plugin.py -TotalCount 120`
>
> - Ran `Select-String -Path backend\open_webui\utils\tools.py -Pattern 'class Tools|def get_tool|def tool|Tools\(|__tools__|get_function|registry|register|load_tool' | Select-Object LineNumber,Line | Format-Table -AutoSize -Wrap`
>
> - Ran `Get-Content backend\open_webui\utils\tools.py | Select-Object -Skip 940 -First 120`
>
> - Ran `Select-String -Path backend\open_webui\utils\plugin.py -Pattern 'def load_tool_module_by_id|def get_tools_cache|def get_tool_contents_cache|class Tools|Tools\(' | Select-Object LineNumber,Line | Format-Table -AutoSize -Wrap`
>
> - Ran `Get-Content backend\open_webui\utils\plugin.py | Select-Object -Skip 200 -First 90`
>
> - Ran `Select-String -Path backend\open_webui\utils\plugin.py -Pattern 'def extract_frontmatter|def replace_imports' | Select-Object LineNumber,Line; Write-Output '--- frontmatter impl ---'; Get-Content backend\open_webui\utils\plugin.py | Select-Object -Skip 130 -First 70`
>
> - Ran `Write-Output '=== data dir ==='; Get-ChildItem -Path data -Force | Select-Object Mode,Length,Name | Format-Table -AutoSize; Write-Output '=== ENABLE_PLUGINS default ==='; Select-String -Path backend\open_webui\env.py -Pattern 'ENABLE_PLUGINS' | Select-Object LineNumber,Line`
>
> - Ran `Write-Output '=== D:\研一\open-webui-main exists? ==='; Test-Path 'D:\研一\open-webui-main'; Write-Output '=== D:\研一\open-webui-main\backend open_webui tools ==='; if (Test-Path 'D:\研一\open-webui-main') { Get-ChildItem 'D:\研一\open-webui-main' -Force | Select-Object Name | Format-Table -AutoSize }`
>
> - Ran `Write-Output '=== data/corpus tree ==='; Get-ChildItem -Path data\corpus -Recurse | Select-Object FullName | Format-Table -AutoSize -Wrap`
>
> - Ran `Get-ChildItem -Path docs -Recurse -Depth 1 | Select-Object FullName | Format-Table -AutoSize -Wrap`
>
> - Ran `Get-Content docs\project-overview.md -TotalCount 200`
>
> - Ran `Get-Content docs\architecture.md -TotalCount 200`
>
> - Ran `Get-Content docs\interface-agreement.md -TotalCount 200`
>
> - Ran `Get-Content docs\interface-agreement.md | Select-Object -Skip 200 -First 160`
>
> <details><summary>Explored 3 files</summary>
>
> - Read `./docs/prompts/ds-assistant-system-prompt.md`
> - Read `./docs/prompts/ds-course-scope.md`
> - Read `./docs/prompts/ds-assistant-model-notes.md`
>
> </details>
>
> - Ran `Select-String -Path backend\open_webui\utils\plugin.py -Pattern "frontmatter.get|frontmatter\[" | Select-Object LineNumber,Line | Format-Table -AutoSize -Wrap`
>
> - Ran `Get-Content backend\open_webui\utils\tools.py | Select-Object -Skip 260 -First 130`
>
> - Ran `Write-Output '=== cwd .venv ==='; Test-Path '.\backend\.venv'; Test-Path '.\backend\venv'; Test-Path '.\.venv'; Write-Output '=== python ==='; python --version 2>&1; Write-Output '=== python modules pydantic ==='; python -c "import pydantic; print('pydantic', pydantic.__version__)" 2>&1`
>
> - Ran `Select-String -Path backend\open_webui\utils\tools.py -Pattern 'def convert_function_to_pydantic_model|def clean_openai_tool_schema' | Select-Object LineNumber,Line; Write-Output '--- convert_function_to_pydantic_model ---'; Get-Content backend\open_webui\utils\tools.py | Select-Object -Skip 800 -First 140`
>
> - Ran `& .\backend\.venv\Scripts\python.exe --version 2>&1; Write-Output '--- pydantic in venv ---'; & .\backend\.venv\Scripts\python.exe -c "import pydantic, open_webui; print('pydantic', pydantic.__version__)" 2>&1`
>
> - Ran `Get-Content .\backend\.venv\pyvenv.cfg; Write-Output '--- Scripts ---'; Get-ChildItem .\backend\.venv\Scripts | Select-Object Name | Format-Table -AutoSize`
>
> - Ran `Write-Output '=== Python311 exists? ==='; Test-Path 'C:\Users\12461\AppData\Local\Programs\Python\Python311\python.exe'; Write-Output '=== Python314 ==='; Test-Path 'C:\Users\12461\AppData\Local\Programs\Python\Python314\python.exe'; Get-Command python | Select-Object Source`
>
> - Ran `& 'C:\Users\12461\AppData\Local\Programs\Python\Python311\python.exe' --version 2>&1; Write-Output '--- venv python direct ---'; & 'D:\暑期实训\小组作业\open-webui-ai\backend\.venv\Scripts\python.exe' --version 2>&1`
>
> - Ran `& 'C:\Users\12461\AppData\Local\Programs\Python\Python311\python.exe' -c "import pydantic; print('pydantic', pydantic.__version__)" 2>&1; Write-Output '--- venv site-packages check ---'; Get-ChildItem .\backend\.venv\Lib\site-packages -Directory -ErrorAction SilentlyContinue | Where-Object { $_.Name -match 'pydantic|openai|fastapi|langchain' } | Select-Object Name | Format-Table -AutoSize`
>
> - Ran `cmd /c "C:\Users\12461\AppData\Local\Programs\Python\Python311\python.exe --version" 2>&1`
>
> - Ran `cmd /c "D:\暑期实训\小组作业\open-webui-ai\backend\.venv\Scripts\python.exe --version" 2>&1; Write-Output '--- dir Python311 ---'; Get-ChildItem 'C:\Users\12461\AppData\Local\Programs\Python\Python311' -Force | Select-Object Mode,Length,Name | Format-Table -AutoSize`
>
> - Ran `& 'C:\Users\12461\miniconda3\python.exe' --version`
>
> - Ran `& 'C:\Users\12461\miniconda3\python.exe' -c "import sys; print(sys.version)"`
>
> - Ran `& 'C:\Users\12461\miniconda3\python.exe' -c "import langchain_core; print('langchain_core', langchain_core.__version__)"`
>
> - Ran `& 'C:\Users\12461\miniconda3\python.exe' -c "from langchain_core.utils.function_calling import convert_to_openai_function; print('OK convert_to_openai_function')"`
>
> - Ran `& 'C:\Users\12461\miniconda3\python.exe' -c "import sys; sys.path.insert(0, r'D:\暑期实训\小组作业\open-webui-ai\backend\.venv\Lib\site-packages'); import langchain_core; print('langchain_core', langchain_core.__version__); from langchain_core.utils.function_calling import convert_to_openai_function; print('OK')"`
>
> - Ran `cmd /c dir "C:\Users\12461\AppData\Local\Programs\Python\Python311" 2>&1`
>
> - Ran `Write-Output '=== port 8080 ==='; try { (Invoke-WebRequest -Uri 'http://localhost:8080/api/v1/config' -TimeoutSec 3 -UseBasicParsing).StatusCode } catch { $_.Exception.Message }; Write-Output '=== port 5173 ==='; try { (Invoke-WebRequest -Uri 'http://localhost:5173' -TimeoutSec 3 -UseBasicParsing).StatusCode } catch { $_.Exception.Message }`
>
> - Ran `Write-Output '=== routers/tools.py ==='; Get-ChildItem backend\open_webui\routers\tools.py | Select-Object Length; Select-String -Path backend\open_webui\routers\tools.py -Pattern '@router|def |ToolForm|ToolModel' | Select-Object LineNumber,Line | Format-Table -AutoSize -Wrap`
>
> - Ran `Write-Output '=== /health ==='; try { (Invoke-WebRequest -Uri 'http://localhost:8080/health' -TimeoutSec 5 -UseBasicParsing).Content } catch { $_.Exception.Message }; Write-Output '=== /api/v1/tools ==='; try { $r = Invoke-WebRequest -Uri 'http://localhost:8080/api/v1/tools/' -TimeoutSec 5 -UseBasicParsing; "Status: $($r.StatusCode)"; $r.Content.Substring(0, [Math]::Min(500, $r.Content.Length)) } catch { "ERR: $($_.Exception.Message)" }`
>
> - Ran `Select-String -Path backend\open_webui\routers\tools.py -Pattern '@router\.(get|post).*(test|run|execute|valves/spec)|def .*(test|run|execute)' | Select-Object LineNumber,Line | Format-Table -AutoSize -Wrap`
>
> - Ran `Get-Content backend\open_webui\routers\tools.py | Select-Object -Skip 344 -First 90`
>
> - Ran `Get-Content data\corpus\91_试卷\数据结构期中试卷及答案解析.md -TotalCount 120`
>
> - Ran `Get-Content data\corpus\90_习题代码\基础63题.md -TotalCount 100`
>
> - Ran `Select-String -Path backend\open_webui\utils\tools.py -Pattern 'result|json.dumps|return str|await callable|tool_result' | Select-Object LineNumber,Line | Format-Table -AutoSize -Wrap`
>
> - Ran `Write-Output '=== docs/tools exists? ==='; Test-Path docs\tools; Write-Output '=== docs listing ==='; Get-ChildItem docs -Recurse -Depth 2 | Select-Object FullName | Format-Table -AutoSize -Wrap`
>
> - Ran `Select-String -Path backend\open_webui\tools\builtin.py -Pattern '^async def |^def ' | Select-Object LineNumber,Line | Format-Table -AutoSize -Wrap | Select-Object -First 60`
>
> - Ran `Get-Content backend\open_webui\tools\builtin.py | Select-Object -Skip 148 -First 40`
>
> - Ran `Get-Content backend\open_webui\utils\tools.py | Select-Object -Skip 195 -First 70`
>
> - Ran `Select-String -Path src\lib\components\workspace\Tools\*.svelte -Pattern 'manifest|title|author|version|description|requirements' | Select-Object LineNumber,Line | Format-Table -AutoSize -Wrap | Select-Object -First 40`
>
> - Ran `Get-ChildItem src\lib\components\workspace -Recurse -Directory | Where-Object { $_.Name -match 'Tool' } | Select-Object FullName | Format-Table -AutoSize -Wrap; Write-Output '--- tools components ---'; Get-ChildItem src\lib\components\workspace\Tools -ErrorAction SilentlyContinue | Select-Object Name | Format-Table -AutoSize`
>
> - Ran `Get-Content src\lib\components\workspace\Tools\ToolkitEditor.svelte | Select-Object -Skip 40 -First 220`
>
> - Ran `Get-Content src\lib\components\workspace\Tools\ToolkitEditor.svelte | Select-Object -Skip 295 -First 60`
>
> - Ran `Select-String -Path src\lib\components\workspace\Tools\ToolkitEditor.svelte -Pattern 'fm\.|frontmatter|extract|import .*frontmatter' | Select-Object LineNumber,Line | Format-Table -AutoSize -Wrap`
>
> </details>
>
> 调研已充分。现在开始实现。先创建目录，然后编写工具源码。
>
> <details><summary>Ran a command</summary>
>
> - Ran `New-Item -ItemType Directory -Force -Path docs\tools | Out-Null; Write-Output 'created'; Get-ChildItem docs\tools | Select-Object Name`
>
> </details>
>
> 现在编写工具源码。这是核心交付物，包含完整的题库与抽题/判分逻辑。
>
> <details><summary>Ran 23 commands</summary>
>
> - Ran `$content = @'
> """
> title: DS Quiz Tool
> author: 组员B（扩展工具负责人）
> version: 1.0.0
> description: 数据结构课程练习题工具：按章节随机抽题 + 客观题自动判分
> requirements:
> """
>
> import json
> import random
> import re
>
> # ---------------------------------------------------------------------------
> # 章节映射
> # ---------------------------------------------------------------------------
>
> CHAPTERS = {
>     "ch01": "第1章 绪论",
>     "ch02": "第2章 线性表",
>     "ch03": "第3章 栈、队列和数组",
>     "ch04": "第4章 串",
>     "ch05": "第5章 树与二叉树",
>     "ch06": "第6章 图",
>     "ch07": "第7章 查找",
>     "ch08": "第8章 排序",
> }
>
> # 主题关键词 -> 章节 key（用于自然语言章节匹配，例如“栈”“二叉树”）
> KEYWORDS = {
>     "绪论": "ch01", "复杂度": "ch01", "算法": "ch01",
>     "线性表": "ch02", "顺序表": "ch02", "链表": "ch02",
>     "栈": "ch03", "队列": "ch03", "数组": "ch03", "矩阵": "ch03",
>     "串": "ch04", "KMP": "ch04", "kmp": "ch04", "模式匹配": "ch04",
>     "树": "ch05", "二叉树": "ch05", "哈夫曼": "ch05", "线索": "ch05",
>     "图": "ch06", "最短路径": "ch06", "拓扑": "ch06", "最小生成树": "ch06",
>     "查找": "ch07", "散列": "ch07", "哈希": "ch07", "折半": "ch07", "二分": "ch07",
>     "排序": "ch08", "堆排": "ch08", "快排": "ch08", "归并": "ch08", "基数": "ch08",
> }
>
>
> def resolve_chapter(text):
>     """把用户输入的章节描述解析为 (chapter_key, chapter_name)，失败返回 (None, None)。"""
>     t = str(text).strip()
>     if not t:
>         return None, None
>
>     # 1) 精确匹配 chapter key
>     if t in CHAPTERS:
>         return t, CHAPTERS[t]
>     
>     # 2) 提取第一个数字（支持“第3章”“3”“chapter 3”等）
>     m = re.search(r"(\d+)", t)
>     if m:
>         n = int(m.group(1))
>         if 1 <= n <= 8:
>             key = f"ch{n:02d}"
>             return key, CHAPTERS[key]
>     
>     # 3) 主题关键词匹配（长的关键词优先，避免“二叉树”被“树”截胡）
>     lower = t.lower()
>     for kw in sorted(KEYWORDS.keys(), key=len, reverse=True):
>         if kw in t or kw.lower() in lower:
>             key = KEYWORDS[kw]
>             return key, CHAPTERS[key]
>     
>     return None, None
>
>
> # ---------------------------------------------------------------------------
> # 题库（32 道客观题，覆盖 8 章；题型 choice/judge/fill）
> # ---------------------------------------------------------------------------
>
> QUESTION_BANK = [
>     # ===== 第1章 绪论 =====
>     {"id": "ch01_001", "chapter": "ch01", "type": "choice", "difficulty": "easy",
>      "stem": "数据结构的三个要素是（　）。",
>      "options": ["A. 数据元素、数据项、数据类型", "B. 逻辑结构、存储结构、数据的运算",
>                  "C. 线性结构、树形结构、图形结构", "D. 顺序存储、链式存储、索引存储"],
>      "answer": "B",
>      "explanation": "数据结构研究的三要素是逻辑结构、存储（物理）结构，以及定义在结构上的数据运算。"},
>     {"id": "ch01_002", "chapter": "ch01", "type": "choice", "difficulty": "medium",
>      "stem": "某算法的时间复杂度为 O(n·logn)，下列说法正确的是（　）。",
>      "options": ["A. 该算法实际执行 n·logn 次基本操作", "B. 当 n 足够大时，其增长阶介于 O(n) 与 O(n²) 之间",
>                  "C. 该算法一定比 O(n) 的算法快", "D. 该算法的空间复杂度也一定是 O(n·logn)"],
>      "answer": "B",
>      "explanation": "大 O 记号描述的是增长阶（渐近上界），O(n·logn) 介于 O(n) 与 O(n²) 之间；它不是精确执行次数，也不代表空间复杂度。"},
>     {"id": "ch01_003", "chapter": "ch01", "type": "judge", "difficulty": "easy",
>      "stem": "数据元素是数据的基本单位，在计算机中通常作为一个整体进行考虑和处理。",
>      "options": [],
>      "answer": "对",
>      "explanation": "数据元素是数据的基本单位；数据项是构成数据元素、不可分割的最小单位。"},
>     {"id": "ch01_004", "chapter": "ch01", "type": "fill", "difficulty": "medium",
>      "stem": "算法的五个重要特性是有穷性、确定性、可行性、输入和____。",
>      "options": [],
>      "answer": "输出",
>      "explanation": "算法的五大特性：有穷性、确定性、可行性、输入、输出；算法可以没有输入，但至少要有一个输出。"},
>
>     # ===== 第2章 线性表 =====
>     {"id": "ch02_001", "chapter": "ch02", "type": "choice", "difficulty": "easy",
>      "stem": "顺序存储结构的线性表，最主要的优点是（　）。",
>      "options": ["A. 插入、删除方便", "B. 可以随机存取任一元素", "C. 存储密度低", "D. 不需要连续的存储空间"],
>      "answer": "B",
>      "explanation": "顺序表用数组连续存储，可按下标直接访问任意元素，即随机存取；其插入删除需移动大量元素，是主要缺点。"},
>     {"id": "ch02_002", "chapter": "ch02", "type": "choice", "difficulty": "medium",
>      "stem": "长度为 n 的顺序表，在第 i（1≤i≤n+1）个位置插入一个新元素，需要移动的元素个数是（　）。",
>      "options": ["A. n-i", "B. n-i+1", "C. n-i-1", "D. i"],
>      "answer": "B",
>      "explanation": "在第 i 个位置插入，需要把第 i 到第 n 共 (n-i+1) 个元素依次后移一位，再放入新元素。"},
>     {"id": "ch02_003", "chapter": "ch02", "type": "judge", "difficulty": "easy",
>      "stem": "顺序存储的线性表支持随机存取，即访问任意元素的时间复杂度为 O(1)。",
>      "options": [],
>      "answer": "对",
>      "explanation": "顺序表按下标直接定位，访问第 i 个元素时间复杂度为 O(1)，即随机存取。"},
>     {"id": "ch02_004", "chapter": "ch02", "type": "fill", "difficulty": "medium",
>      "stem": "在单链表中删除某个结点时，通常需要先找到该结点的____结点。",
>      "options": [],
>      "answer": "前驱",
>      "explanation": "单链表结点只有指向后继的指针，删除结点需修改其前驱结点的 next 指针，因此要先定位前驱。"},
>     
>     # ===== 第3章 栈、队列和数组 =====
>     {"id": "ch03_001", "chapter": "ch03", "type": "choice", "difficulty": "easy",
>      "stem": "栈的特点是（　）。",
>      "options": ["A. 先进先出", "B. 后进先出", "C. 随机存取", "D. 只允许在一端进、另一端出"],
>      "answer": "B",
>      "explanation": "栈是只允许在一端进行插入和删除的线性表，特点是后进先出（LIFO）。"},
>     {"id": "ch03_002", "chapter": "ch03", "type": "choice", "difficulty": "medium",
>      "stem": "用一维数组实现循环队列时，通常少用一个存储单元来区分队空与队满。若队头指针 front 指向队头元素、队尾指针 rear 指向队尾元素的下一个位置，则队满的条件是（　）。",
>      "options": ["A. front == rear", "B. (rear+1) % maxSize == front", "C. rear == front+1", "D. (rear-1) % maxSize == front"],
>      "answer": "B",
>      "explanation": "牺牲一个单元区分队空队满：队空为 front==rear，队满为 (rear+1)%maxSize==front。"},
>     {"id": "ch03_003", "chapter": "ch03", "type": "judge", "difficulty": "easy",
>      "stem": "栈和队列都是操作受限的线性表。",
>      "options": [],
>      "answer": "对",
>      "explanation": "栈和队列本质都是线性表，只是插入、删除操作的位置受到限制。"},
>     {"id": "ch03_004", "chapter": "ch03", "type": "fill", "difficulty": "medium",
>      "stem": "括号匹配、表达式求值和递归调用等场景，通常使用____这种数据结构来实现。",
>      "options": [],
>      "answer": "栈",
>      "explanation": "栈的后进先出特性天然适合括号匹配、表达式求值以及函数递归调用时的现场保存与恢复。"},
>     
>     # ===== 第4章 串 =====
>     {"id": "ch04_001", "chapter": "ch04", "type": "choice", "difficulty": "easy",
>      "stem": "下列关于空串和空格串的说法，正确的是（　）。",
>      "options": ["A. 空串的长度为 1", "B. 空格串的长度为 0", "C. 空串是不含任何字符的串，长度为 0", "D. 空串与空格串完全相同"],
>      "answer": "C",
>      "explanation": "空串不含任何字符、长度为 0；空格串由一个或多个空格组成、长度不为 0，二者不同。"},
>     {"id": "ch04_002", "chapter": "ch04", "type": "choice", "difficulty": "medium",
>      "stem": "KMP 算法相对于朴素模式匹配算法，最主要的改进是（　）。",
>      "options": ["A. 主串指针 i 不回溯", "B. 模式串长度减半", "C. 不需要比较字符", "D. 使用链表存储主串"],
>      "answer": "A",
>      "explanation": "KMP 借助 next 数组，在失配时保持主串指针 i 不回退，只移动模式串指针 j，避免重复比较。"},
>     {"id": "ch04_003", "chapter": "ch04", "type": "judge", "difficulty": "easy",
>      "stem": "KMP 算法中，主串的匹配指针不会回溯。",
>      "options": [],
>      "answer": "对",
>      "explanation": "KMP 的核心思想就是利用 next 数组使主串指针 i 只前进不后退。"},
>     {"id": "ch04_004", "chapter": "ch04", "type": "fill", "difficulty": "medium",
>      "stem": "KMP 算法中，next 数组记录的是：当模式串第 j 个字符与主串失配时，模式串应回退到的位置，其实质是模式串的最长相等____。",
>      "options": [],
>      "answer": "前后缀",
>      "explanation": "next[j] 表示模式串子串 [0..j-1] 的最长相等前后缀长度，失配时据此跳过已匹配部分。"},
>     
>     # ===== 第5章 树与二叉树 =====
>     {"id": "ch05_001", "chapter": "ch05", "type": "choice", "difficulty": "easy",
>      "stem": "一棵二叉树的第 i（i≥1）层上最多有（　）个结点。",
>      "options": ["A. 2^i", "B. 2^(i-1)", "C. 2^(i-1)-1", "D. 2^i-1"],
>      "answer": "B",
>      "explanation": "二叉树第 i 层结点数最多为 2^(i-1) 个（根为第 1 层）。"},
>     {"id": "ch05_002", "chapter": "ch05", "type": "choice", "difficulty": "medium",
>      "stem": "已知一棵完全二叉树共有 626 个结点，则叶子结点的个数为（　）。",
>      "options": ["A. 311", "B. 312", "C. 313", "D. 314"],
>      "answer": "C",
>      "explanation": "完全二叉树中，n0=n2+1 且 n=n0+n1+n2，其中 n1 为 0 或 1；对偶数个结点 n1=1，解得 n0=313。"},
>     {"id": "ch05_003", "chapter": "ch05", "type": "judge", "difficulty": "medium",
>      "stem": "已知一棵二叉树的先序遍历序列和中序遍历序列，可以唯一确定这棵二叉树。",
>      "options": [],
>      "answer": "对",
>      "explanation": "先序（或后序）+ 中序可唯一确定二叉树；但仅先序+后序不能唯一确定。"},
>     {"id": "ch05_004", "chapter": "ch05", "type": "fill", "difficulty": "medium",
>      "stem": "哈夫曼树的带权路径长度 WPL 定义为：所有叶子结点的____与其到根路径长度的乘积之和。",
>      "options": [],
>      "answer": "权值",
>      "explanation": "WPL = Σ(叶子权值 × 该叶子到根的路径长度)，哈夫曼树是 WPL 最小的二叉树。"},
>     
>     # ===== 第6章 图 =====
>     {"id": "ch06_001", "chapter": "ch06", "type": "choice", "difficulty": "easy",
>      "stem": "具有 n 个顶点、e 条边的无向图，所有顶点的度数之和为（　）。",
>      "options": ["A. n", "B. e", "C. 2e", "D. 2n"],
>      "answer": "C",
>      "explanation": "无向图中每条边连接两个顶点、贡献 2 个度，因此度数之和 = 2e。"},
>     {"id": "ch06_002", "chapter": "ch06", "type": "choice", "difficulty": "medium",
>      "stem": "Dijkstra 算法用于求解（　）。",
>      "options": ["A. 单源最短路径（边权非负）", "B. 任意两点间的最短路径（含负权）", "C. 最小生成树", "D. 拓扑排序"],
>      "answer": "A",
>      "explanation": "Dijkstra 算法求单源最短路径，要求边权非负；含负权边时需用 Bellman-Ford，任意两点间可用 Floyd。"},
>     {"id": "ch06_003", "chapter": "ch06", "type": "judge", "difficulty": "easy",
>      "stem": "对连通图进行深度优先遍历（DFS）或广度优先遍历（BFS），可以访问到图中所有顶点。",
>      "options": [],
>      "answer": "对",
>      "explanation": "连通图中任意两点连通，从任一顶点出发进行 DFS 或 BFS 均可遍历全部顶点。"},
>     {"id": "ch06_004", "chapter": "ch06", "type": "fill", "difficulty": "medium",
>      "stem": "一个连通无向图的生成树是包含图中全部顶点的极小连通子图，其中边权之和最小的生成树称为____。",
>      "options": [],
>      "answer": "最小生成树",
>      "explanation": "边权之和最小的生成树即最小生成树（MST），可用 Prim 或 Kruskal 算法构造。"},
>     
>     # ===== 第7章 查找 =====
>     {"id": "ch07_001", "chapter": "ch07", "type": "choice", "difficulty": "easy",
>      "stem": "折半查找（二分查找）要求查找表必须是（　）。",
>      "options": ["A. 顺序存储且有序", "B. 链式存储且有序", "C. 顺序存储但无序", "D. 任意存储结构"],
>      "answer": "A",
>      "explanation": "折半查找依赖随机存取并按中间元素比较来缩小区间，要求顺序存储且元素有序。"},
>     {"id": "ch07_002", "chapter": "ch07", "type": "choice", "difficulty": "medium",
>      "stem": "下列不属于散列表冲突处理方法的是（　）。",
>      "options": ["A. 拉链法", "B. 开放定址法", "C. 再散列法", "D. 折半查找法"],
>      "answer": "D",
>      "explanation": "处理冲突常用拉链法、开放定址法、再散列法（建立公共溢出区）等；折半查找是查找方法，不是冲突处理方法。"},
>     {"id": "ch07_003", "chapter": "ch07", "type": "judge", "difficulty": "easy",
>      "stem": "对二叉排序树进行中序遍历，可以得到一个递增有序序列。",
>      "options": [],
>      "answer": "对",
>      "explanation": "二叉排序树左<根<右，中序遍历（左-根-右）恰好输出递增序列。"},
>     {"id": "ch07_004", "chapter": "ch07", "type": "fill", "difficulty": "medium",
>      "stem": "衡量查找算法效率的主要指标是____查找长度（ASL）。",
>      "options": [],
>      "answer": "平均",
>      "explanation": "平均查找长度 ASL = Σ(查找第 i 个记录的概率 × 查找该记录所需比较次数)，是评价查找效率的主要指标。"},
>     
>     # ===== 第8章 排序 =====
>     {"id": "ch08_001", "chapter": "ch08", "type": "choice", "difficulty": "easy",
>      "stem": "快速排序在最坏情况下的时间复杂度为（　）。",
>      "options": ["A. O(n)", "B. O(n·logn)", "C. O(n²)", "D. O(logn)"],
>      "answer": "C",
>      "explanation": "当每次划分极不平衡（如序列基本有序）时，快速排序退化为 O(n²)；平均时间复杂度为 O(n·logn)。"},
>     {"id": "ch08_002", "chapter": "ch08", "type": "choice", "difficulty": "medium",
>      "stem": "下列排序算法中，属于稳定排序的是（　）。",
>      "options": ["A. 快速排序", "B. 堆排序", "C. 归并排序", "D. 简单选择排序"],
>      "answer": "C",
>      "explanation": "归并排序、冒泡排序、直接插入排序是稳定的；快速排序、堆排序、简单选择排序、希尔排序不稳定。"},
>     {"id": "ch08_003", "chapter": "ch08", "type": "judge", "difficulty": "easy",
>      "stem": "归并排序是一种稳定的排序算法，其时间复杂度为 O(n·logn)。",
>      "options": [],
>      "answer": "对",
>      "explanation": "归并排序在合并时保持相等元素的相对次序，是稳定排序，时间复杂度稳定为 O(n·logn)。"},
>     {"id": "ch08_004", "chapter": "ch08", "type": "fill", "difficulty": "medium",
>      "stem": "堆排序中，将无序序列构造成一个大根堆的过程称为____。",
>      "options": [],
>      "answer": "建堆",
>      "explanation": "把无序数组调整为堆的过程叫建堆（初始化堆），堆排序先建堆再反复交换堆顶与末尾并向下调整。"},
> ]
>
>
> # ---------------------------------------------------------------------------
> # 辅助函数
> # ---------------------------------------------------------------------------
>
> def _to_bool(value):
>     if isinstance(value, bool):
>         return value
>     return str(value).strip().lower() in ("true", "1", "yes", "是", "对", "y", "t")
>
>
> def _normalize_fill(text):
>     t = str(text).strip().lower()
>     t = re.sub(r"[，。；、,.!！?？\s]", "", t)
>     return t
>
>
> def _normalize_judge(text):
>     t = str(text).strip().lower()
>     if t in ("对", "正确", "是", "t", "true", "√", "yes", "y"):
>         return "对"
>     if t in ("错", "错误", "否", "f", "false", "×", "no", "n"):
>         return "错"
>     return t
>
>
> def _answers_match(user, correct, qtype):
>     if qtype == "choice":
>         return str(user).strip().upper() == str(correct).strip().upper()
>     if qtype == "judge":
>         return _normalize_judge(user) == _normalize_judge(correct)
>     # fill：归一化后做包含匹配（双向，取较宽松的一侧）
>     u = _normalize_fill(user)
>     c = _normalize_fill(correct)
>     if not u or not c:
>         return False
>     return u in c or c in u
>
>
> class Tools:
>     def __init__(self):
>         self.bank = QUESTION_BANK
>
>     def ds_random_quiz(
>         self,
>         chapter: str,
>         question_count: int = 5,
>         question_type: str = "choice",
>         difficulty: str = "all",
>         include_answer: bool = False,
>     ) -> str:
>         """
>         按章节随机抽取数据结构客观题，用于自测或复习。默认只返回题目（不含答案），学生作答后可调用 ds_grade_quiz 判分。
>     
>         :param chapter: 章节，可用“第3章”“3”“ch03”或主题词（如“栈”“二叉树”“排序”）
>         :param question_count: 抽题数量，默认 5
>         :param question_type: 题型，可选 choice(选择)/judge(判断)/fill(填空)/all(全部)，默认 choice
>         :param difficulty: 难度，可选 easy/medium/hard/all，默认 all
>         :param include_answer: 是否在出题时直接附带答案与解析，默认 False（自测模式不泄露答案）
>         """
>         key, name = resolve_chapter(chapter)
>         if key is None:
>             return json.dumps(
>                 {
>                     "ok": False,
>                     "error": {
>                         "code": "INVALID_CHAPTER",
>                         "message": "未找到该章节。请使用“第1章~第8章”、数字 1~8、或章节主题词（如“栈”“二叉树”“排序”）。",
>                     },
>                 },
>                 ensure_ascii=False,
>             )
>     
>         include_answer = _to_bool(include_answer)
>     
>         pool = [q for q in self.bank if q["chapter"] == key]
>         if question_type != "all":
>             pool = [q for q in pool if q["type"] == question_type]
>         if difficulty != "all":
>             pool = [q for q in pool if q["difficulty"] == difficulty]
>     
>         if not pool:
>             return json.dumps(
>                 {
>                     "ok": False,
>                     "error": {
>                         "code": "NO_QUESTION",
>                         "message": f"章节“{name}”中没有符合条件（题型={question_type}，难度={difficulty}）的题目。",
>                     },
>                 },
>                 ensure_ascii=False,
>             )
>     
>         count = int(question_count)
>         if count <= 0:
>             count = 1
>         count = min(count, len(pool))
>         selected = random.sample(pool, count)
>     
>         questions = []
>         for q in selected:
>             item = {
>                 "id": q["id"],
>                 "type": q["type"],
>                 "difficulty": q["difficulty"],
>                 "stem": q["stem"],
>             }
>             if q["type"] == "choice":
>                 item["options"] = q["options"]
>             if include_answer:
>                 item["answer"] = q["answer"]
>                 item["explanation"] = q["explanation"]
>             questions.append(item)
>     
>         return json.dumps(
>             {
>                 "ok": True,
>                 "data": {
>                     "chapter": name,
>                     "question_count": count,
>                     "questions": questions,
>                     "note": "请逐题作答。判分时调用 ds_grade_quiz，把上面的 questions 原样传回，并按顺序给出答案数组。" if not include_answer else None,
>                 },
>             },
>             ensure_ascii=False,
>         )
>     
>     def ds_grade_quiz(self, questions: str, answers: str) -> str:
>         """
>         对学生提交的客观题答案进行自动判分，并给出每题解析与总分。
>     
>         :param questions: 题目列表的 JSON 字符串（即 ds_random_quiz 返回的 data.questions 数组，每道题需含 id 或 stem）
>         :param answers: 学生答案的 JSON 数组字符串，按题目顺序，例如 '["B","对","栈"]'
>         """
>         try:
>             qs = json.loads(questions) if isinstance(questions, str) else questions
>         except Exception:
>             return json.dumps(
>                 {"ok": False, "error": {"code": "INVALID_QUESTIONS", "message": "questions 不是合法的 JSON 数组。"}},
>                 ensure_ascii=False,
>             )
>         if not isinstance(qs, list):
>             return json.dumps(
>                 {"ok": False, "error": {"code": "INVALID_QUESTIONS", "message": "questions 必须是数组。"}},
>                 ensure_ascii=False,
>             )
>     
>         try:
>             ans = json.loads(answers) if isinstance(answers, str) else answers
>         except Exception:
>             # 容错：允许直接传单个答案字符串
>             ans = [answers]
>         if not isinstance(ans, list):
>             ans = [ans]
>     
>         if len(qs) != len(ans):
>             return json.dumps(
>                 {
>                     "ok": False,
>                     "error": {
>                         "code": "LENGTH_MISMATCH",
>                         "message": f"题目数量({len(qs)})与答案数量({len(ans)})不一致，请按题目顺序逐题给出答案。",
>                     },
>                 },
>                 ensure_ascii=False,
>             )
>     
>         results = []
>         correct_count = 0
>         for idx, (q, a) in enumerate(zip(qs, ans), start=1):
>             qid = q.get("id") if isinstance(q, dict) else None
>             qtype = q.get("type") if isinstance(q, dict) else "choice"
>             stem = q.get("stem", "") if isinstance(q, dict) else str(q)
>     
>             target = None
>             # 先按 id 从题库查找
>             for item in self.bank:
>                 if item["id"] == qid:
>                     target = item
>                     break
>             # 找不到 id 时按 stem 匹配
>             if target is None and stem:
>                 for item in self.bank:
>                     if item["stem"] == stem or item["stem"] in stem or stem in item["stem"]:
>                         target = item
>                         break
>     
>             if target is None:
>                 results.append({
>                     "index": idx,
>                     "question_id": qid,
>                     "stem": stem,
>                     "user_answer": a,
>                     "correct": False,
>                     "graded": False,
>                     "message": "题库中未找到该题，无法判分。请确认题目来自本工具 ds_random_quiz 的返回结果。",
>                 })
>                 continue
>     
>             qtype = target["type"]
>             is_correct = _answers_match(a, target["answer"], qtype)
>             if is_correct:
>                 correct_count += 1
>             results.append({
>                 "index": idx,
>                 "question_id": target["id"],
>                 "type": qtype,
>                 "stem": target["stem"],
>                 "user_answer": a,
>                 "correct_answer": target["answer"],
>                 "correct": is_correct,
>                 "graded": True,
>                 "explanation": target["explanation"],
>             })
>     
>         total = len(qs)
>         score = round(correct_count / total * 100, 1) if total else 0.0
>     
>         return json.dumps(
>             {
>                 "ok": True,
>                 "data": {
>                     "total": total,
>                     "correct_count": correct_count,
>                     "wrong_count": total - correct_count,
>                     "score": score,
>                     "results": results,
>                 },
>             },
>             ensure_ascii=False,
>         )
> '@
> Set-Content -LiteralPath 'docs\tools\ds_quiz_tool.py' -Value $content -Encoding UTF8
> Write-Output "written, length=$($content.Length)"`
> - Ran `& 'C:\Users\12461\miniconda3\python.exe' -c "import ast; ast.parse(open(r'docs/tools/ds_quiz_tool.py', encoding='utf-8').read()); print('SYNTAX OK')"`
> - Ran `& 'C:\Users\12461\miniconda3\python.exe' -c "import sys; sys.path.insert(0, 'docs/tools'); import ds_quiz_tool as m; t = m.Tools(); print('IMPORT OK, bank size =', len(t.bank))"`
> - Ran `& 'C:\Users\12461\miniconda3\python.exe' -c @"
> import sys, json
> sys.path.insert(0, 'docs/tools')
> import ds_quiz_tool as m
> t = m.Tools()
>
> # resolve_chapter
> for s in ['第3章','3','ch05','栈','二叉树','排序','ch07']:
>     print(s, '->', m.resolve_chapter(s))
>
> # random quiz
> r = json.loads(t.ds_random_quiz(chapter='第3章', question_count=3, question_type='all'))
> print('quiz ok=', r['ok'], 'count=', r['data']['question_count'], 'keys=', list(r['data']['questions'][0].keys()))
>
> # full chapter pool
> r2 = json.loads(t.ds_random_quiz(chapter='ch05', question_count=99, question_type='all', include_answer=True))
> print('ch05 all questions =', r2['data']['question_count'])
>
> # invalid chapter
> r3 = json.loads(t.ds_random_quiz(chapter='操作系统'))
> print('invalid ->', r3)
> "@`
> - Ran `$env:PYTHONIOENCODING='utf-8'; & 'C:\Users\12461\miniconda3\python.exe' -c @"
> import sys, json
> sys.path.insert(0, 'docs/tools')
> import ds_quiz_tool as m
> t = m.Tools()
>
> # 抽题（含答案）用于构造判分输入
> r = json.loads(t.ds_random_quiz(chapter='第2章', question_count=4, question_type='all', include_answer=True))
> qs = r['data']['questions']
> print('questions:', [(q['id'], q['type'], q['answer']) for q in qs])
>
> # 构造答案：全部按标准答案
> answers = [q['answer'] for q in qs]
> g = json.loads(t.ds_grade_quiz(json.dumps(qs, ensure_ascii=False), json.dumps(answers, ensure_ascii=False)))
> print('grade all correct -> total=%s correct=%s score=%s' % (g['data']['total'], g['data']['correct_count'], g['data']['score']))
>
> # 全部答错
> wrong = ['A','错','x','y']
> g2 = json.loads(t.ds_grade_quiz(json.dumps(qs, ensure_ascii=False), json.dumps(wrong, ensure_ascii=False)))
> print('grade all wrong -> correct=%s score=%s' % (g2['data']['correct_count'], g2['data']['score']))
> for it in g2['data']['results']:
>     print('  ', it['question_id'], 'user=', it['user_answer'], 'correct=', it['correct'], 'graded=', it['graded'])
>
> # 数量不匹配
> g3 = json.loads(t.ds_grade_quiz(json.dumps(qs, ensure_ascii=False), json.dumps(['B'], ensure_ascii=False)))
> print('mismatch ->', g3['error']['code'])
>
> # 非法 questions
> g4 = json.loads(t.ds_grade_quiz('not-json', json.dumps(['B'], ensure_ascii=False)))
> print('invalid q ->', g4['error']['code'])
> "@`
> - Ran `$env:PYTHONIOENCODING='utf-8'; & 'C:\Users\12461\miniconda3\python.exe' -c @"
> import sys, json, inspect
> from typing import get_type_hints
> from pydantic import create_model
> sys.path.insert(0, 'docs/tools')
> import ds_quiz_tool as m
> t = m.Tools()
>
> def convert(func):
>     type_hints = get_type_hints(func)
>     sig = inspect.signature(func)
>     field_defs = {}
>     for name, param in sig.parameters.items():
>         if name == 'self':
>             continue
>         th = type_hints.get(name, str)
>         default = param.default if param.default is not inspect.Parameter.empty else ...
>         field_defs[name] = (th, default)
>     return create_model(func.__name__, **field_defs)
>
> for fn in [t.ds_random_quiz, t.ds_grade_quiz]:
>     model = convert(fn)
>     schema = model.model_json_schema()
>     print('===', fn.__name__, '===')
>     print('required:', schema.get('required'))
>     for k, v in schema['properties'].items():
>         ann = v.get('anyOf') or v.get('type') or v
>         print(' ', k, '->', v.get('type') if 'type' in v else [a.get('type') for a in v.get('anyOf',[])], 'default=', v.get('default', '<required>'))
> "@`
> - Ran `Select-String -Path backend\open_webui\routers\auths.py -Pattern '@router\.(get|post)' | Select-Object LineNumber,Line | Format-Table -AutoSize -Wrap | Select-Object -First 40`
> - Ran `Write-Output '=== find webui.db ==='; Get-ChildItem -Path . -Recurse -Filter 'webui.db' -ErrorAction SilentlyContinue | Select-Object FullName,Length | Format-Table -AutoSize -Wrap; Write-Output '=== DATA_DIR env? check backend process ==='; Get-CimInstance Win32_Process -Filter "name like '%python%' or name like '%uvicorn%'" | Select-Object ProcessId,Name,CommandLine | Format-Table -AutoSize -Wrap`
> - Ran `& 'C:\Users\12461\miniconda3\python.exe' -c @"
> import sqlite3
> conn = sqlite3.connect(r'backend/data/webui.db')
> conn.row_factory = sqlite3.Row
> cur = conn.cursor()
> cur.execute(\"SELECT name FROM sqlite_master WHERE type='table' AND name LIKE '%user%'\")
> print('tables:', [r[0] for r in cur.fetchall()])
> for t in ['user']:
>     try:
>         cur.execute(f'PRAGMA table_info({t})')
>         cols = [r[1] for r in cur.fetchall()]
>         print(t, 'cols:', cols)
>     except Exception as e:
>         print('err', e)
> conn.close()
> "@`
> - Ran `& 'C:\Users\12461\miniconda3\python.exe' -c @"
> import sqlite3
> conn = sqlite3.connect(r'backend/data/webui.db')
> cur = conn.cursor()
> cur.execute("SELECT name FROM sqlite_master WHERE type='table'")
> tables = [r[0] for r in cur.fetchall()]
> print('tables:', tables)
> for t in tables:
>     if 'user' in t.lower():
>         cur.execute(f'PRAGMA table_info("{t}")')
>         print(t, 'cols:', [r[1] for r in cur.fetchall()])
> conn.close()
> "@`
> - Ran `& 'C:\Users\12461\miniconda3\python.exe' -c @"
> import sqlite3
> conn = sqlite3.connect(r'backend/data/webui.db')
> conn.row_factory = sqlite3.Row
> cur = conn.cursor()
> print('--- users ---')
> for r in cur.execute('SELECT id, name, email, role FROM user'):
>     print(dict(r))
> print('--- api_key count ---')
> for r in cur.execute('SELECT count(*) c FROM api_key'):
>     print('api_key rows:', r['c'])
> print('--- tools ---')
> for r in cur.execute('SELECT id, name FROM tool'):
>     print(dict(r))
> print('--- models ---')
> for r in cur.execute('SELECT id, name, base_model_id FROM model'):
>     print(dict(r))
> conn.close()
> "@`
> - Ran `Get-Content backend\open_webui\routers\auths.py | Select-Object -Skip 885 -First 60`
> - Ran `$body = @{ name='ds-tool-test'; email='ds_tool_test@local.test'; password='[REDACTED_SECRET]' } | ConvertTo-Json
> try {
>   $r = Invoke-WebRequest -Uri 'http://localhost:8080/api/v1/auths/signup' -Method Post -Body $body -ContentType 'application/json' -TimeoutSec 10 -UseBasicParsing
>   "Status: $($r.StatusCode)"
>   $r.Content
> } catch {
>   "ERR: $($_.Exception.Message)"
>   if ($_.ErrorDetails.Message) { $_.ErrorDetails.Message }
> }`
> - Ran `Write-Output '=== netstat 8080 ==='; netstat -ano | Select-String ':8080' | Select-Object -First 5; Write-Output '=== netstat 5173 ==='; netstat -ano | Select-String ':5173' | Select-Object -First 5`
> - Ran `Get-CimInstance Win32_Process -Filter "ProcessId=12304" | Select-Object ProcessId,ExecutablePath,CommandLine | Format-List`
> - Ran `& 'C:\Users\12461\miniconda3\python.exe' -c @"
> import sys
> base = r'D:\暑期实训\小组作业\open-webui-ai\backend\.venv\Lib\site-packages'
> sys.path.insert(0, base + r'\langchain_protocol')
> sys.path.insert(0, base + r'\langchain_core')
> import pydantic_core
> print('pydantic_core from miniconda:', pydantic_core.__version__)
> try:
>     from langchain_core.utils.function_calling import convert_to_openai_function
>     print('OK convert_to_openai_function')
> except Exception as e:
>     import traceback; traceback.print_exc()
> "@`
> - Ran `& 'C:\Users\12461\miniconda3\python.exe' -c @"
> import pydantic, pydantic_core, sys
> print('pydantic', pydantic.__version__)
> print('pydantic_core', pydantic_core.__version__)
> print('pydantic_core file:', pydantic_core.__file__)
> print('pydantic file:', pydantic.__file__)
> print('sys.executable:', sys.executable)
> "@`
> - Ran `& 'C:\Users\12461\miniconda3\python.exe' -c @"
> import sys, shutil, tempfile, os
> base = r'D:\暑期实训\小组作业\open-webui-ai\backend\.venv\Lib\site-packages'
> tmp = tempfile.mkdtemp()
> for pkg in ['langchain_core', 'langchain_protocol']:
>     src = os.path.join(base, pkg)
>     if os.path.isdir(src):
>         shutil.copytree(src, os.path.join(tmp, pkg))
> sys.path.insert(0, tmp)
> try:
>     from langchain_core.utils.function_calling import convert_to_openai_function
>     print('OK convert_to_openai_function loaded')
> except Exception as e:
>     import traceback; traceback.print_exc()
> finally:
>     shutil.rmtree(tmp, ignore_errors=True)
> "@`
> - Ran `$env:PYTHONIOENCODING='utf-8'; & 'C:\Users\12461\miniconda3\python.exe' -c @"
> import sys, os, shutil, tempfile, inspect, re, copy, json, types
> from typing import get_type_hints, get_args
> from pydantic import BaseModel, Field, create_model
>
> # 加载 langchain_core 的 convert_to_openai_function（从 venv 复制，避免 C 扩展冲突）
> base = r'D:\暑期实训\小组作业\open-webui-ai\backend\.venv\Lib\site-packages'
> tmp = tempfile.mkdtemp()
> for pkg in ['langchain_core', 'langchain_protocol']:
>     shutil.copytree(os.path.join(base, pkg), os.path.join(tmp, pkg))
> sys.path.insert(0, tmp)
> from langchain_core.utils.function_calling import convert_to_openai_function as _convert
>
> # 复刻 open_webui/utils/tools.py 的核心
> def parse_description(docstring):
>     if not docstring: return ''
>     lines = [l.strip() for l in docstring.strip().split('\n')]
>     out = []
>     for line in lines:
>         if re.match(r':param', line) or re.match(r':return', line): break
>         out.append(line)
>     return '\n'.join(out)
>
> def parse_docstring(docstring):
>     if not docstring: return {}
>     d = {}
>     for line in docstring.splitlines():
>         m = re.match(r':param (\w+):\s*(.+)', line.strip())
>         if m and not m.group(1).startswith('__'):
>             d[m.group(1)] = m.group(2)
>     return d
>
> def convert_function_to_pydantic_model(func):
>     type_hints = get_type_hints(func)
>     sig = inspect.signature(func)
>     params = sig.parameters
>     desc = parse_description(func.__doc__)
>     pdesc = parse_docstring(func.__doc__)
>     fields = {}
>     for name, param in params.items():
>         th = type_hints.get(name, str)
>         default = param.default if param.default is not inspect.Parameter.empty else ...
>         if name in pdesc:
>             fields[name] = (th, Field(default, description=pdesc[name]))
>         else:
>             fields[name] = (th, default)
>     model = create_model(func.__name__, **fields)
>     model.__doc__ = desc
>     return model
>
> def clean_properties(schema):
>     if not isinstance(schema, dict): return
>     if 'anyOf' in schema:
>         non_null = [t for t in schema['anyOf'] if t.get('type') != 'null']
>         if len(non_null) == 1:
>             schema.update(non_null[0]); del schema['anyOf']
>         else:
>             schema['anyOf'] = non_null
>     if 'default' in schema and schema['default'] is None: del schema['default']
>     if 'type' not in schema and 'anyOf' not in schema and 'properties' not in schema:
>         schema['type'] = 'string'
>     if 'properties' in schema:
>         for p in schema['properties'].values(): clean_properties(p)
>     if 'items' in schema: clean_properties(schema['items'])
>
> def clean_openai_tool_schema(spec):
>     s = copy.deepcopy(spec)
>     if 'parameters' in s: clean_properties(s['parameters'])
>     return s
>
> # 加载工具模块（复刻 load_tool_module_by_id 的 exec 逻辑）
> src = open(r'docs/tools/ds_quiz_tool.py', encoding='utf-8').read()
> module = types.ModuleType('tool_ds_quiz')
> module.__dict__['__file__'] = 'ds_quiz_tool.py'
> exec(compile(src, 'ds_quiz_tool.py', 'exec'), module.__dict__)
> tool = module.Tools()
>
> # 提取公共方法 -> 生成 spec
> funcs = [getattr(tool, f) for f in dir(tool)
>          if callable(getattr(tool, f)) and not f.startswith('_') and not inspect.isclass(getattr(tool, f))]
> print('=== 工具函数 ===')
> for fn in funcs:
>     model = convert_function_to_pydantic_model(fn)
>     spec = _convert(model)
>     spec = clean_openai_tool_schema(spec)
>     print(json.dumps({'name': spec['name'], 'description': spec['description'], 'parameters': spec['parameters']}, ensure_ascii=False, indent=2))
> "@`
> - Ran `$base='D:\暑期实训\小组作业\open-webui-ai\backend\.venv\Lib\site-packages\langchain_core\utils\function_calling.py'; Get-Content $base | Select-Object -Skip 380 -First 60`
> - Ran `$base='D:\暑期实训\小组作业\open-webui-ai\backend\.venv\Lib\site-packages\langchain_core\utils\function_calling.py'; Get-Content $base | Select-Object -Skip 438 -First 90`
> - Ran `$base='D:\暑期实训\小组作业\open-webui-ai\backend\.venv\Lib\site-packages\langchain_core\utils\function_calling.py'; Select-String -Path $base -Pattern '_convert_pydantic_to_openai_function|def _convert_pydantic' | Select-Object LineNumber,Line; Write-Output '--- body ---'; Get-Content $base | Select-Object -Skip 100 -First 120`
> - Ran `$env:PYTHONIOENCODING='utf-8'; & 'C:\Users\12461\miniconda3\python.exe' -c @"
> import sys, inspect, re, copy, json, types
> from typing import get_type_hints
> from pydantic import BaseModel, Field, create_model
>
> # ===== 复刻 Open WebUI + langchain_core 的 spec 生成逻辑 =====
> def parse_description(docstring):
>     if not docstring: return ''
>     lines = [l.strip() for l in docstring.strip().split('\n')]
>     out = []
>     for line in lines:
>         if re.match(r':param', line) or re.match(r':return', line): break
>         out.append(line)
>     return '\n'.join(out)
>
> def parse_docstring(docstring):
>     if not docstring: return {}
>     d = {}
>     for line in docstring.splitlines():
>         m = re.match(r':param (\w+):\s*(.+)', line.strip())
>         if m and not m.group(1).startswith('__'):
>             d[m.group(1)] = m.group(2)
>     return d
>
> def convert_function_to_pydantic_model(func):
>     type_hints = get_type_hints(func)
>     sig = inspect.signature(func)
>     desc = parse_description(func.__doc__)
>     pdesc = parse_docstring(func.__doc__)
>     fields = {}
>     for name, param in sig.parameters.items():
>         th = type_hints.get(name, str)
>         default = param.default if param.default is not inspect.Parameter.empty else ...
>         if name in pdesc:
>             fields[name] = (th, Field(default, description=pdesc[name]))
>         else:
>             fields[name] = (th, default)
>     model = create_model(func.__name__, **fields)
>     model.__doc__ = desc
>     return model
>
> def _rm_titles(kv, prev_key=None):
>     new_kv = {}
>     for k, v in kv.items():
>         if k == 'title':
>             if isinstance(v, dict) and prev_key == 'properties':
>                 new_kv[k] = _rm_titles(v, k)
>             else:
>                 continue
>         elif isinstance(v, dict):
>             new_kv[k] = _rm_titles(v, k)
>         else:
>             new_kv[k] = v
>     return new_kv
>
> def _convert_pydantic_to_openai_function(model):
>     schema = model.model_json_schema()
>     if '$defs' in schema: schema.pop('$defs', None)
>     title = schema.pop('title', '')
>     default_description = schema.pop('description', '')
>     return {'name': title, 'description': default_description, 'parameters': _rm_titles(schema)}
>
> def clean_properties(schema):
>     if not isinstance(schema, dict): return
>     if 'anyOf' in schema:
>         non_null = [t for t in schema['anyOf'] if t.get('type') != 'null']
>         if len(non_null) == 1:
>             schema.update(non_null[0]); del schema['anyOf']
>         else:
>             schema['anyOf'] = non_null
>     if 'default' in schema and schema['default'] is None: del schema['default']
>     if 'type' not in schema and 'anyOf' not in schema and 'properties' not in schema:
>         schema['type'] = 'string'
>     if 'properties' in schema:
>         for p in schema['properties'].values(): clean_properties(p)
>     if 'items' in schema: clean_properties(schema['items'])
>
> def clean_openai_tool_schema(spec):
>     s = copy.deepcopy(spec)
>     if 'parameters' in s: clean_properties(s['parameters'])
>     return s
>
> # ===== 加载工具模块 =====
> src = open(r'docs/tools/ds_quiz_tool.py', encoding='utf-8').read()
> module = types.ModuleType('tool_ds_quiz'); module.__dict__['__file__']='ds_quiz_tool.py'
> exec(compile(src, 'ds_quiz_tool.py', 'exec'), module.__dict__)
> tool = module.Tools()
>
> funcs = [getattr(tool, f) for f in dir(tool)
>          if callable(getattr(tool, f)) and not f.startswith('_') and not inspect.isclass(getattr(tool, f))]
> print('公共方法数 =', len(funcs))
> for fn in funcs:
>     spec = clean_openai_tool_schema(_convert_pydantic_to_openai_function(convert_function_to_pydantic_model(fn)))
>     print('='*60)
>     print(json.dumps(spec, ensure_ascii=False, indent=2))
> "@`
>
> </details>
>
> 工具 spec 生成验证通过。现在编写正式的自动化测试脚本（交付物）。
>
> <details><summary>Ran 4 commands</summary>
>
> - Ran `$content = @'
> """
> 数据结构课程练习题工具 —— 本地离线测试脚本。
>
> 用途：不依赖 Open WebUI，直接对 ds_quiz_tool.py 的抽题与判分逻辑做单元测试。
> 运行：python test_ds_quiz_tool.py
> """
>
> import json
> import os
> import sys
> import unittest
>
> sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))
>
> import ds_quiz_tool as m  # noqa: E402
>
>
> def run(func, **kwargs):
>     return json.loads(func(**kwargs))
>
>
> class TestResolveChapter(unittest.TestCase):
>     def test_numeric(self):
>         self.assertEqual(m.resolve_chapter("第3章")[0], "ch03")
>         self.assertEqual(m.resolve_chapter("3")[0], "ch03")
>         self.assertEqual(m.resolve_chapter("ch05")[0], "ch05")
>         self.assertEqual(m.resolve_chapter("chapter 7")[0], "ch07")
>
>     def test_keyword(self):
>         self.assertEqual(m.resolve_chapter("栈和队列")[0], "ch03")
>         self.assertEqual(m.resolve_chapter("二叉树")[0], "ch05")
>         self.assertEqual(m.resolve_chapter("排序算法")[0], "ch08")
>     
>     def test_invalid(self):
>         self.assertEqual(m.resolve_chapter("操作系统"), (None, None))
>         self.assertEqual(m.resolve_chapter(""), (None, None))
>
>
> class TestRandomQuiz(unittest.TestCase):
>     def setUp(self):
>         self.t = m.Tools()
>
>     def test_basic(self):
>         r = run(self.t.ds_random_quiz, chapter="第3章", question_count=3, question_type="all")
>         self.assertTrue(r["ok"])
>         self.assertEqual(r["data"]["question_count"], 3)
>         self.assertEqual(len(r["data"]["questions"]), 3)
>         # 自测模式默认不含答案
>         self.assertNotIn("answer", r["data"]["questions"][0])
>     
>     def test_include_answer(self):
>         r = run(self.t.ds_random_quiz, chapter="ch02", question_count=4, question_type="all", include_answer=True)
>         self.assertTrue(r["ok"])
>         for q in r["data"]["questions"]:
>             self.assertIn("answer", q)
>             self.assertIn("explanation", q)
>     
>     def test_type_filter(self):
>         r = run(self.t.ds_random_quiz, chapter="第5章", question_count=99, question_type="choice")
>         self.assertTrue(r["ok"])
>         for q in r["data"]["questions"]:
>             self.assertEqual(q["type"], "choice")
>     
>     def test_difficulty_filter(self):
>         r = run(self.t.ds_random_quiz, chapter="第8章", question_count=99, question_type="all", difficulty="easy")
>         self.assertTrue(r["ok"])
>         for q in r["data"]["questions"]:
>             self.assertEqual(q["difficulty"], "easy")
>     
>     def test_invalid_chapter(self):
>         r = run(self.t.ds_random_quiz, chapter="机器学习")
>         self.assertFalse(r["ok"])
>         self.assertEqual(r["error"]["code"], "INVALID_CHAPTER")
>     
>     def test_question_count_clamped(self):
>         # 每章只有 4 题，请求 100 题应被限制为实际题数
>         r = run(self.t.ds_random_quiz, chapter="第1章", question_count=100, question_type="all")
>         self.assertTrue(r["ok"])
>         self.assertEqual(r["data"]["question_count"], 4)
>
>
> class TestGradeQuiz(unittest.TestCase):
>     def setUp(self):
>         self.t = m.Tools()
>         self.r = run(self.t.ds_random_quiz, chapter="第2章", question_count=4, question_type="all", include_answer=True)
>         self.qs = self.r["data"]["questions"]
>
>     def _grade(self, answers):
>         return run(
>             self.t.ds_grade_quiz,
>             questions=json.dumps(self.qs, ensure_ascii=False),
>             answers=json.dumps(answers, ensure_ascii=False),
>         )
>     
>     def test_all_correct(self):
>         g = self._grade([q["answer"] for q in self.qs])
>         self.assertTrue(g["ok"])
>         self.assertEqual(g["data"]["correct_count"], 4)
>         self.assertEqual(g["data"]["score"], 100.0)
>     
>     def test_all_wrong(self):
>         g = self._grade(["A", "错", "x", "y"])
>         self.assertTrue(g["ok"])
>         self.assertEqual(g["data"]["correct_count"], 0)
>         for it in g["data"]["results"]:
>             self.assertFalse(it["correct"])
>             self.assertTrue(it["graded"])
>             self.assertIn("explanation", it)
>     
>     def test_mixed(self):
>         correct = [q["answer"] for q in self.qs]
>         wrong = ["A", "错", "x", "y"]
>         g = self._grade([correct[0], wrong[1], wrong[2], wrong[3]])
>         self.assertEqual(g["data"]["correct_count"], 1)
>     
>     def test_length_mismatch(self):
>         g = self._grade(["B"])
>         self.assertFalse(g["ok"])
>         self.assertEqual(g["error"]["code"], "LENGTH_MISMATCH")
>     
>     def test_invalid_questions_json(self):
>         g = run(self.t.ds_grade_quiz, questions="not-json", answers='["B"]')
>         self.assertFalse(g["ok"])
>         self.assertEqual(g["error"]["code"], "INVALID_QUESTIONS")
>     
>     def test_grade_by_stem_without_id(self):
>         # 去掉 id，仅凭 stem 也应能判分
>         qs_no_id = [{k: v for k, v in q.items() if k != "id"} for q in self.qs]
>         g = run(
>             self.t.ds_grade_quiz,
>             questions=json.dumps(qs_no_id, ensure_ascii=False),
>             answers=json.dumps([q["answer"] for q in self.qs], ensure_ascii=False),
>         )
>         self.assertTrue(g["ok"])
>         self.assertEqual(g["data"]["correct_count"], 4)
>     
>     def test_normalization_choice_case_insensitive(self):
>         # 选择题答案大小写不敏感：正确答案 B，提交 b
>         choice = [q for q in self.qs if q["type"] == "choice"]
>         self.assertTrue(choice)
>         g = run(
>             self.t.ds_grade_quiz,
>             questions=json.dumps(choice, ensure_ascii=False),
>             answers=json.dumps([q["answer"].lower() for q in choice], ensure_ascii=False),
>         )
>         self.assertEqual(g["data"]["correct_count"], len(choice))
>
>
> class TestAnswerNormalization(unittest.TestCase):
>     def test_judge(self):
>         self.assertTrue(m._answers_match("对", "对", "judge"))
>         self.assertTrue(m._answers_match("正确", "对", "judge"))
>         self.assertTrue(m._answers_match("T", "对", "judge"))
>         self.assertTrue(m._answers_match("错", "错", "judge"))
>         self.assertFalse(m._answers_match("对", "错", "judge"))
>
>     def test_fill(self):
>         self.assertTrue(m._answers_match("栈", "栈", "fill"))
>         self.assertTrue(m._answers_match("平均查找长度", "平均", "fill"))
>         self.assertFalse(m._answers_match("队列", "栈", "fill"))
>
>
> if __name__ == "__main__":
>     unittest.main(verbosity=2)
> '@
> Set-Content -LiteralPath 'docs\tools\test_ds_quiz_tool.py' -Value $content -Encoding UTF8
> Write-Output "written test_ds_quiz_tool.py"`
> - Ran `$env:PYTHONIOENCODING='utf-8'; & 'C:\Users\12461\miniconda3\python.exe' 'docs\tools\test_ds_quiz_tool.py'`
> - Ran `$env:PYTHONIOENCODING='utf-8'; & 'C:\Users\12461\miniconda3\python.exe' -c @"
> import sys, json
> sys.path.insert(0, 'docs/tools')
> import ds_quiz_tool as m
> t = m.Tools()
> r = json.loads(t.ds_random_quiz(chapter='第2章', question_count=4, question_type='all', include_answer=True))
> qs = r['data']['questions']
> for q in qs:
>     print(q['id'], q['type'], 'ans=', q['answer'], '| stem=', q['stem'][:20])
> wrong = ['A','错','x','y']
> g = json.loads(t.ds_grade_quiz(json.dumps(qs, ensure_ascii=False), json.dumps(wrong, ensure_ascii=False)))
> for it in g['data']['results']:
>     print(it['question_id'], 'user=', it['user_answer'], 'correct_ans=', it['correct_answer'], 'correct=', it['correct'])
> print('correct_count=', g['data']['correct_count'])
> "@`
> - Ran `$env:PYTHONIOENCODING='utf-8'; & 'C:\Users\12461\miniconda3\python.exe' -c @"
> import sys, json
> sys.path.insert(0, 'docs/tools')
> import ds_quiz_tool as m
> t = m.Tools()
> wrong = ['A','错','x','y']
> for i in range(200):
>     r = json.loads(t.ds_random_quiz(chapter='第2章', question_count=4, question_type='all', include_answer=True))
>     qs = r['data']['questions']
>     g = json.loads(t.ds_grade_quiz(json.dumps(qs, ensure_ascii=False), json.dumps(wrong, ensure_ascii=False)))
>     if g['data']['correct_count'] != 0:
>         print('iter', i, 'correct_count=', g['data']['correct_count'])
>         for it in g['data']['results']:
>             print('   ', it['question_id'], it['type'], 'user=', it['user_answer'], 'ans=', it['correct_answer'], 'correct=', it['correct'])
>         break
> else:
>     print('all 200 iterations correct_count=0')
> "@`
>
> </details>
>
> 发现两个真实问题：`_normalize_judge` 把单字母 `y`/`n` 误判，且关键词 `算法` 太宽泛。修复这两处。
>
> <details><summary>Ran 3 commands</summary>
>
> - Ran `& 'C:\Users\12461\miniconda3\python.exe' -c @"
> p = r'docs/tools/ds_quiz_tool.py'
> s = open(p, encoding='utf-8').read()
>
> # 1) 关键词：去掉过宽泛的“算法”，增加具体复杂度词
> old_kw = '    \"绪论\": \"ch01\", \"复杂度\": \"ch01\", \"算法\": \"ch01\",'
> new_kw = '    \"绪论\": \"ch01\", \"复杂度\": \"ch01\", \"时间复杂度\": \"ch01\", \"空间复杂度\": \"ch01\",'
> assert old_kw in s, 'kw not found'
> s = s.replace(old_kw, new_kw)
>
> # 2) judge 归一化：去掉单字母 y/n（易误判）
> old_j = '    if t in (\"对\", \"正确\", \"是\", \"t\", \"true\", \"√\", \"yes\", \"y\"):\n        return \"对\"\n    if t in (\"错\", \"错误\", \"否\", \"f\", \"false\", \"×\", \"no\", \"n\"):\n        return \"错\"'
> new_j = '    if t in (\"对\", \"正确\", \"是\", \"t\", \"true\", \"√\", \"yes\"):\n        return \"对\"\n    if t in (\"错\", \"错误\", \"否\", \"f\", \"false\", \"×\", \"no\"):\n        return \"错\"'
> assert old_j in s, 'judge block not found'
> s = s.replace(old_j, new_j)
>
> open(p, 'w', encoding='utf-8').write(s)
> print('patched OK')
> "@`
> - Ran `& 'C:\Users\12461\miniconda3\python.exe' -c @"
> p = r'docs/tools/test_ds_quiz_tool.py'
> s = open(p, encoding='utf-8').read()
>
> old = '''    def test_all_wrong(self):
>         g = self._grade([\"A\", \"错\", \"x\", \"y\"])
>         self.assertTrue(g[\"ok\"])
>         self.assertEqual(g[\"data\"][\"correct_count\"], 0)
>         for it in g[\"data\"][\"results\"]:
>             self.assertFalse(it[\"correct\"])
>             self.assertTrue(it[\"graded\"])
>             self.assertIn(\"explanation\", it)
>
>     def test_mixed(self):
>         correct = [q[\"answer\"] for q in self.qs]
>         wrong = [\"A\", \"错\", \"x\", \"y\"]
>         g = self._grade([correct[0], wrong[1], wrong[2], wrong[3]])
>         self.assertEqual(g[\"data\"][\"correct_count\"], 1)
> '''
>
> new = '''    @staticmethod
>     def _wrong_answer(q):
>         if q[\"type\"] == \"choice\":
>             for letter in (\"A\", \"B\", \"C\", \"D\"):
>                 if letter != q[\"answer\"]:
>                     return letter
>         if q[\"type\"] == \"judge\":
>             return \"错\" if q[\"answer\"] == \"对\" else \"对\"
>         return \"与正确答案无关的文本zz\"
>
>     def test_all_wrong(self):
>         wrong = [self._wrong_answer(q) for q in self.qs]
>         g = self._grade(wrong)
>         self.assertTrue(g[\"ok\"])
>         self.assertEqual(g[\"data\"][\"correct_count\"], 0)
>         for it in g[\"data\"][\"results\"]:
>             self.assertFalse(it[\"correct\"])
>             self.assertTrue(it[\"graded\"])
>             self.assertIn(\"explanation\", it)
>     
>     def test_mixed(self):
>         correct = [q[\"answer\"] for q in self.qs]
>         wrong = [self._wrong_answer(q) for q in self.qs]
>         g = self._grade([correct[0], wrong[1], wrong[2], wrong[3]])
>         self.assertEqual(g[\"data\"][\"correct_count\"], 1)
> '''
>
> assert old in s, 'old block not found'
> s = s.replace(old, new)
> open(p, 'w', encoding='utf-8').write(s)
> print('test patched OK')
> "@`
> - Ran `$env:PYTHONIOENCODING='utf-8'; & 'C:\Users\12461\miniconda3\python.exe' 'docs\tools\test_ds_quiz_tool.py'`
>
> </details>
>
> 18 个测试全部通过。现在编写工具元数据登记与使用说明文档。
>
> <details><summary>Ran 5 commands</summary>
>
> - Ran ````$content = @'
> # 扩展工具目录（Tool Catalog）
>
> > 版本：v1.0
> > 维护人：组员B（扩展工具负责人）
> > 关联文档：`docs/interface-agreement.md` 第 7 节、`docs/tools/tool-usage.md`、`docs/tools/ds_quiz_tool.py`
>
> 本文件登记「数据结构 AI 助教」所挂接的扩展工具元数据，供助教集成负责人与测试负责人对齐。
>
> ## 1. 工具概览
>
> | 字段        | 值                                                           |
> | ----------- | ------------------------------------------------------------ |
> | tool_id     | ds_quiz                                                      |
> | tool_name   | 数据结构练习题（抽题 + 判分）                                |
> | tool_type   | Workspace Tool                                               |
> | description | 按章节随机抽取数据结构客观题（选择/判断/填空），并对学生提交的答案自动判分、给出解析与得分 |
> | version     | v1.0                                                         |
> | owner       | 组员B                                                        |
> | status      | ready                                                        |
>
> ## 2. 工具函数
>
> 工具源码内定义 `class Tools`，共暴露两个函数（即两个可被模型调用的工具）：
>
> | 函数名           | 用途                                     |
> | ---------------- | ---------------------------------------- |
> | `ds_random_quiz` | 章节随机抽题（默认不附答案，供自测）     |
> | `ds_grade_quiz`  | 客观题自动判分（逐题判定 + 解析 + 总分） |
>
> ## 3. 输入输出结构
>
> ### 3.1 ds_random_quiz（抽题）
>
> **输入（OpenAI function calling 参数）**
>
> | 参数           | 类型    | 必填 | 默认值 | 说明                                                       |
> | -------------- | ------- | ---- | ------ | ---------------------------------------------------------- |
> | chapter        | string  | 是   | —      | 章节，支持“第3章”“3”“ch03”或主题词（如“栈”“二叉树”“排序”） |
> | question_count | integer | 否   | 5      | 抽题数量                                                   |
> | question_type  | string  | 否   | choice | choice/judge/fill/all                                      |
> | difficulty     | string  | 否   | all    | easy/medium/hard/all                                       |
> | include_answer | boolean | 否   | false  | 是否在出题时附带答案与解析（自测模式为 false）             |
>
> **输出（JSON 字符串）**
>
> ```json
> {
>   "ok": true,
>   "data": {
>     "chapter": "第3章 栈、队列和数组",
>     "question_count": 3,
>     "questions": [
>       {
>         "id": "ch03_001",
>         "type": "choice",
>         "difficulty": "easy",
>         "stem": "栈的特点是（　）。",
>         "options": ["A. 先进先出", "B. 后进先出", "C. 随机存取", "D. 只允许在一端进、另一端出"]
>       }
>     ]
>   }
> }
> ```
>
> ### 3.2 ds_grade_quiz（判分）
>
> **输入**
>
> | 参数      | 类型   | 必填 | 说明                                                         |
> | --------- | ------ | ---- | ------------------------------------------------------------ |
> | questions | string | 是   | `ds_random_quiz` 返回的 `data.questions` 数组的 JSON 字符串  |
> | answers   | string | 是   | 学生答案的 JSON 数组字符串，按题目顺序，如 `'["B","对","栈"]'` |
>
> **输出**
>
> ```json
> {
>   "ok": true,
>   "data": {
>     "total": 3,
>     "correct_count": 2,
>     "wrong_count": 1,
>     "score": 66.7,
>     "results": [
>       {
>         "index": 1,
>         "question_id": "ch03_001",
>         "type": "choice",
>         "stem": "栈的特点是（　）。",
>         "user_answer": "B",
>         "correct_answer": "B",
>         "correct": true,
>         "graded": true,
>         "explanation": "栈是只允许在一端进行插入和删除的线性表，特点是后进先出（LIFO）。"
>       }
>     ]
>   }
> }
> ```
>
> ### 3.3 错误返回（统一结构）
>
> ```json
> {"ok": false, "error": {"code": "INVALID_CHAPTER", "message": "未找到该章节……"}}
> ```
>
> | code              | 含义                         |
> | ----------------- | ---------------------------- |
> | INVALID_CHAPTER   | 章节无法识别                 |
> | NO_QUESTION       | 该章节没有符合条件的题目     |
> | INVALID_QUESTIONS | questions 不是合法 JSON 数组 |
> | LENGTH_MISMATCH   | 题目数量与答案数量不一致     |
>
> ## 4. 自然语言触发示例
>
> 1. “帮我从第 3 章随机抽 5 道选择题，难度中等。”
> 2. “从第 2 章抽 3 道题，题型不限，先不要给答案。”
> 3. “把这几道题判一下分，我的答案是 B、对、栈。”
> 4. “第 5 章二叉树章节，抽 4 道题并附上答案和解析。”
>
> ## 5. 题库范围
>
> 内嵌题库共 **32 道客观题**，覆盖第 1~8 章，每章 4 题（2 道选择 + 1 道判断 + 1 道填空），知识点与 `docs/prompts/ds-course-scope.md` 的章节清单一致。
>
> ## 6. 变更记录
>
> | 版本                                                         | 日期       | 变更内容                                    | 维护人 |
> | ------------------------------------------------------------ | ---------- | ------------------------------------------- | ------ |
> | v1.0                                                         | 2026-09-04 | 初版：章节抽题 + 客观题判分，内嵌 32 题题库 | 组员B  |
> | '@                                                           |            |                                             |        |
> | Set-Content -LiteralPath 'docs\tools\tool-catalog.md' -Value $content -Encoding UTF8 |            |                                             |        |
> | Write-Output 'written tool-catalog.md'````                   |            |                                             |        |
> - Ran ````$content = @'
> # 扩展工具使用说明与测试样例
>
> > 工具 ID：`ds_quiz`
> > 源码：`docs/tools/ds_quiz_tool.py`
> > 测试脚本：`docs/tools/test_ds_quiz_tool.py`
> > 维护人：组员B
>
> ## 1. 工具能做什么
>
> 「数据结构练习题」工具提供两个能力，均为**真实结构化输入输出**（非提示词模拟）：
>
> 1. **章节随机抽题**（`ds_random_quiz`）：从内嵌题库按章节、题型、难度随机抽题，默认不返回答案，方便学生自测。
> 2. **客观题自动判分**（`ds_grade_quiz`）：接收题目与学生的答案列表，逐题判定对错、给出标准答案与解析，并汇总得分。
>
> 题库覆盖第 1~8 章共 32 道题（选择 / 判断 / 填空），知识点与课程章节清单一致。
>
> ## 2. 接入助教（Workspace Tool）
>
> ### 方式 A：界面创建（推荐）
>
> 1. 打开前端 http://localhost:5173 ，进入 **Workspace → Tools**。
> 2. 点击新建工具，将 `docs/tools/ds_quiz_tool.py` 的**全部内容**粘贴到代码编辑器。
> 3. 工具名建议填「数据结构练习题」，工具 ID 填 `ds_quiz`（前端会根据 frontmatter 的 `title` 自动生成，需手动核对为英文小写）。
> 4. 保存。前端会自动读取 frontmatter 的 `description` 填入工具简介。
> 5. 在 **Workspace → Models → 数据结构 AI 助教** 的 Tools 字段中挂接 `ds_quiz`。
>
> ### 方式 B：API 创建
>
> ```bash
> curl -X POST http://localhost:8080/api/v1/tools/create \
>   -H "Authorization: Bearer <TOKEN>" \
>   -H "Content-Type: application/json" \
>   -d '{
>     "id": "ds_quiz",
>     "name": "数据结构练习题（抽题 + 判分）",
>     "content": "<ds_quiz_tool.py 的全部源码>",
>     "meta": {"description": "按章节随机抽取数据结构客观题并自动判分"},
>     "access_grants": []
>   }'
> ```
>
> > 创建接口会校验 `id` 只能为字母数字下划线并转为小写；源码中的 frontmatter 会被自动解析为 `meta.manifest`。
>
> ## 3. 测试样例
>
> ### 样例 1：章节随机抽题
>
> **自然语言触发**：从第 3 章随机抽 3 道选择题，先不要给答案。
>
> 模型调用工具：`ds_random_quiz(chapter="第3章", question_count=3, question_type="choice")`
>
> 工具返回（JSON）：
>
> ```json
> {
>   "ok": true,
>   "data": {
>     "chapter": "第3章 栈、队列和数组",
>     "question_count": 3,
>     "questions": [
>       {"id": "ch03_001", "type": "choice", "difficulty": "easy", "stem": "栈的特点是（　）。", "options": ["A. 先进先出", "B. 后进先出", "C. 随机存取", "D. 只允许在一端进、另一端出"]},
>       {"id": "ch03_002", "type": "choice", "difficulty": "medium", "stem": "循环队列队满条件……", "options": ["A. front==rear", "B. (rear+1)%maxSize==front", "C. rear==front+1", "D. (rear-1)%maxSize==front"]},
>       {"id": "ch03_003", "type": "choice", "difficulty": "easy", "stem": "……", "options": ["……"]}
>     ]
>   }
> }
> ```
>
> ### 样例 2：客观题自动判分
>
> **自然语言触发**：帮我判分，我的答案是 B、B、对。
>
> 模型调用工具：`ds_grade_quiz(questions='<样例1返回的 questions JSON>', answers='["B","B","对"]')`
>
> 工具返回（JSON）：
>
> ```json
> {
>   "ok": true,
>   "data": {
>     "total": 3,
>     "correct_count": 3,
>     "wrong_count": 0,
>     "score": 100.0,
>     "results": [
>       {"index": 1, "question_id": "ch03_001", "user_answer": "B", "correct_answer": "B", "correct": true, "graded": true, "explanation": "栈是只允许在一端插入删除的线性表，后进先出（LIFO）。"},
>       {"index": 2, "question_id": "ch03_002", "user_answer": "B", "correct_answer": "B", "correct": true, "graded": true, "explanation": "……"},
>       {"index": 3, "question_id": "ch03_003", "user_answer": "对", "correct_answer": "对", "correct": true, "graded": true, "explanation": "……"}
>     ]
>   }
> }
> ```
>
> ### 样例 3：错误输入（异常处理）
>
> **自然语言触发**：从「操作系统」章节抽 3 道题。
>
> 工具返回：
>
> ```json
> {"ok": false, "error": {"code": "INVALID_CHAPTER", "message": "未找到该章节。请使用“第1章~第8章”、数字 1~8、或章节主题词（如“栈”“二叉树”“排序”）。"}}
> ```
>
> ## 4. 本地测试结果
>
> 运行 `python docs/tools/test_ds_quiz_tool.py`，共 18 个用例全部通过：
>
> ```text
> Ran 18 tests in 0.003s
> OK
> ```
>
> 覆盖点：章节解析（数字/中文/主题词/非法）、抽题（题型过滤/难度过滤/数量钳制/附答案）、判分（全对/全错/部分对/数量不匹配/非法 JSON/按题干匹配/大小写归一）。
>
> ## 5. 与测试负责人的衔接
>
> - 工具调用类测试用例可直接复用第 3 节样例。
> - 判分结果含每题 `correct`、`correct_answer`、`explanation`，便于自动判分与人工复核。
> - 工具返回统一采用 `{"ok": ..., "data": ...}` / `{"ok": false, "error": {...}}` 结构，与 `docs/interface-agreement.md` 第 7 节一致。
> '@
> Set-Content -LiteralPath 'docs\tools\tool-usage.md' -Value $content -Encoding UTF8
> Write-Output 'written tool-usage.md'````
> - Ran `Get-Content start-dev.bat; Write-Output '=== .env files ==='; Get-ChildItem -Path . -Recurse -Filter '.env*' -Force -ErrorAction SilentlyContinue | Select-Object FullName | Format-Table -AutoSize -Wrap; Write-Output '=== search password hints in config ==='; & 'C:\Users\12461\miniconda3\python.exe' -c @"
> import sqlite3
> conn = sqlite3.connect(r'backend/data/webui.db')
> conn.row_factory = sqlite3.Row
> cur = conn.cursor()
> print('--- config keys ---')
> for r in cur.execute('SELECT id, data FROM config LIMIT 50'):
>     print(r['id'])
> conn.close()
> "@`
> - Ran `git status --short; Write-Output '=== current branch ==='; git branch --show-current; Write-Output '=== recent log ==='; git log --oneline -5`
> - Ran `Get-Content backend\requirements-core.txt -ErrorAction SilentlyContinue | Select-Object -First 30; Write-Output '=== size ==='; (Get-Item backend\requirements-core.txt -ErrorAction SilentlyContinue).Length`
>
> </details>
>
> 交付物已齐全。按接口约定创建 `codex/tool` 分支并提交。
>
> <details><summary>Ran a command</summary>
>
> - Ran `git checkout -b codex/tool 2>&1; Write-Output '=== add ==='; git add docs/tools/ds_quiz_tool.py docs/tools/test_ds_quiz_tool.py docs/tools/tool-catalog.md docs/tools/tool-usage.md 2>&1; Write-Output '=== status ==='; git status --short`
>
> </details>
>
> git 写入 `.git` 需要提权。请求执行分支创建与提交。
>
> <details><summary>Explored a file, ran 7 commands</summary>
>
> - Ran `git checkout -b codex/tool && git add docs/tools/ds_quiz_tool.py docs/tools/test_ds_quiz_tool.py docs/tools/tool-catalog.md docs/tools/tool-usage.md && git commit -m "feat(tool): 新增数据结构抽题与判分扩展工具"`
>
> - Ran `git log -3 --format='%an <%ae>' 2>&1; Write-Output '=== local config ==='; git config user.name 2>&1; git config user.email 2>&1`
>
> - Ran `git config user.name "组员B" && git config user.email "member-b@team.local" && git commit -m "feat(tool): 新增数据结构抽题与判分扩展工具"`
>
> - Ran `Get-ChildItem backend -Force | Select-Object Mode,Length,Name | Format-Table -AutoSize`
>
> <details><summary>Explored a file</summary>
>
> - Read `./backend/start-backend.bat`
>
> </details>
>
> - Ran `Get-ChildItem -Path . -Recurse -Include '*.log' -Force -ErrorAction SilentlyContinue | Select-Object FullName,Length,LastWriteTime | Format-Table -AutoSize -Wrap | Select-Object -First 20`
>
> - Ran `conda env list 2>&1`
>
> - Ran `& 'C:\Users\12461\miniconda3\envs\openwebui\python.exe' --version; Write-Output '--- import open_webui tools ---'; & 'C:\Users\12461\miniconda3\envs\openwebui\python.exe' -c "import pydantic; print('pydantic', pydantic.__version__); import open_webui; print('open_webui OK')"`
>
> </details>
>
> 用后端真实运行的 conda 环境做权威联调验证（加载工具、生成 spec、执行函数）。
>
> <details><summary>Ran 2 commands</summary>
>
> - Ran `$env:PYTHONIOENCODING='utf-8'; $py='C:\Users\12461\miniconda3\envs\openwebui\python.exe'; & $py -c @"
> import asyncio, json, sys
> sys.path.insert(0, r'D:\暑期实训\小组作业\open-webui-ai\backend')
> from open_webui.utils.plugin import load_tool_module_by_id
> from open_webui.utils.tools import get_tool_specs
>
> src = open(r'D:\暑期实训\小组作业\open-webui-ai\docs\tools\ds_quiz_tool.py', encoding='utf-8').read()
>
> async def main():
>     tool, frontmatter = await load_tool_module_by_id('ds_quiz', content=src)
>     print('frontmatter:', frontmatter)
>     specs = get_tool_specs(tool)
>     print('工具函数数:', len(specs))
>     for s in specs:
>         print('  name=', s['name'], '| description=', s['description'][:30])
>         print('  params=', json.dumps(s['parameters'], ensure_ascii=False))
>
>     # 实际执行工具函数（模拟模型调用）
>     r1 = tool.ds_random_quiz(chapter='第3章', question_count=2, question_type='all')
>     print('ds_random_quiz ->', r1)
>     
>     # 构造判分输入
>     data = json.loads(r1)['data']['questions']
>     answers = []
>     for q in data:
>         # 从题库取正确答案
>         from open_webui.utils.plugin import load_tool_module_by_id as _l
>         pass
>     # 直接再抽一次带答案的来构造判分
>     r2 = json.loads(tool.ds_random_quiz(chapter='第3章', question_count=2, question_type='all', include_answer=True))
>     qs = r2['data']['questions']
>     ans = [q['answer'] for q in qs]
>     g = tool.ds_grade_quiz(json.dumps(qs, ensure_ascii=False), json.dumps(ans, ensure_ascii=False))
>     print('ds_grade_quiz ->', g)
>
> asyncio.run(main())
> "@`
> - Ran `$env:PYTHONIOENCODING='utf-8'; $env:WEBUI_SECRET_KEY='b8e1c94f6d2a7e3f0c5d8a1b4e7f9c2d'; $py='C:\Users\12461\miniconda3\envs\openwebui\python.exe'; & $py -c @"
> import asyncio, json, sys
> sys.path.insert(0, r'D:\暑期实训\小组作业\open-webui-ai\backend')
> from open_webui.utils.plugin import load_tool_module_by_id
> from open_webui.utils.tools import get_tool_specs
>
> src = open(r'D:\暑期实训\小组作业\open-webui-ai\docs\tools\ds_quiz_tool.py', encoding='utf-8').read()
>
> async def main():
>     tool, frontmatter = await load_tool_module_by_id('ds_quiz', content=src)
>     print('frontmatter:', json.dumps(frontmatter, ensure_ascii=False))
>     specs = get_tool_specs(tool)
>     print('工具函数数:', len(specs))
>     for s in specs:
>         print('  name=', s['name'], '| desc=', s['description'][:25])
>         print('  params=', json.dumps(s['parameters'], ensure_ascii=False))
>
>     r1 = json.loads(tool.ds_random_quiz(chapter='第3章', question_count=2, question_type='all'))
>     print('抽题 ok=', r1['ok'], 'count=', r1['data']['question_count'])
>     
>     r2 = json.loads(tool.ds_random_quiz(chapter='第3章', question_count=2, question_type='all', include_answer=True))
>     qs = r2['data']['questions']
>     ans = [q['answer'] for q in qs]
>     g = json.loads(tool.ds_grade_quiz(json.dumps(qs, ensure_ascii=False), json.dumps(ans, ensure_ascii=False)))
>     print('判分 ok=', g['ok'], 'score=', g['data']['score'], 'correct=', g['data']['correct_count'])
>
> asyncio.run(main())
> "@`
>
> </details>
>
> 用真实运行环境完成权威验证。最后补一份组员B工作记录，便于归档与交互记录整理。
>
> <details><summary>Ran 3 commands</summary>
>
> - Ran ``$content = @'
> # 组员B · 扩展工具工作记录
>
> > 角色：扩展工具负责人
> > 课程：数据结构
> > 日期：2026-09-04
>
> ## 1. 调研结论：Open WebUI 扩展开发方式
>
> | 扩展类型        | 定位                                                         | 是否适合本任务                 |
> | --------------- | ------------------------------------------------------------ | ------------------------------ |
> | Workspace Tool  | 原生函数级工具调用，定义 `Tools` 类 + Python 方法，自动生成 OpenAI function spec | ✅ 最适合（真实结构化输入输出） |
> | Skill           | 给模型的说明性提示 + 资源，偏向指导而非确定性计算            | 不满足“仅提示词模拟不计分”     |
> | Functions（旧） | 早期插件形式，已被 Tools 取代                                | 不采用                         |
> | 外部 API / MCP  | 需要额外服务部署                                             | 本阶段不采用                   |
>
> 关键机制（`backend/open_webui/utils/plugin.py` + `utils/tools.py`）：
>
> - 工具源码存数据库 `tool.content`，必须含 `class Tools`。
> - `Tools` 类的公共方法（非 `_` 开头、callable）即工具函数。
> - 方法参数用类型注解 + docstring（`:param name: desc`）生成 schema。
> - 方法返回 JSON 字符串给模型。
> - 文件头部用 `"""` 包裹 YAML frontmatter（`title/author/version/description/requirements`）。
>
> ## 2. 交付物清单
>
> | 文件                              | 说明                                                         |
> | --------------------------------- | ------------------------------------------------------------ |
> | `docs/tools/ds_quiz_tool.py`      | 工具源码：`ds_random_quiz`（章节抽题）+ `ds_grade_quiz`（自动判分），内嵌 32 题题库 |
> | `docs/tools/test_ds_quiz_tool.py` | 18 个单元测试（章节解析/抽题/判分/边界/异常）                |
> | `docs/tools/tool-catalog.md`      | 工具元数据与输入输出 schema 登记                             |
> | `docs/tools/tool-usage.md`        | 使用说明、接入步骤、测试样例                                 |
>
> ## 3. 验证证据
>
> 1. **单元测试**：`python docs/tools/test_ds_quiz_tool.py` → `Ran 18 tests ... OK`。
> 2. **真实环境加载验证**：用后端运行的同款 conda 环境（`openwebui`，Python 3.11.16）调用 `open_webui.utils.plugin.load_tool_module_by_id` 成功加载工具，`get_tool_specs` 生成 2 个函数 spec 正确，`ds_random_quiz` / `ds_grade_quiz` 实际执行返回正常（判分 score=100）。
> 3. 返回结构符合 `docs/interface-agreement.md` 第 7 节（`ok/data` 与 `ok/error` 统一格式）。
>
> ## 4. 待办（转组长/集成负责人）
>
> - 在 Workspace → Tools 粘贴 `ds_quiz_tool.py` 创建工具（ID `ds_quiz`）。
> - 在「数据结构 AI 助教」模型的 Tools 字段挂接 `ds_quiz`。
> - 与 DeepSeek 进行端到端对话调用验证（需管理员凭据）。
> '@
> Set-Content -LiteralPath 'docs\tools\worklog-member-b.md' -Value $content -Encoding UTF8
> Write-Output 'written worklog-member-b.md'``
> - Ran `git add docs/tools/worklog-member-b.md && git commit -m "docs(tool): 补充组员B扩展工具工作记录"`
> - Ran `git status --short; Write-Output '=== branch ==='; git branch --show-current; Write-Output '=== my commits ==='; git log --oneline -3; Write-Output '=== deliverables ==='; Get-ChildItem docs\tools | Select-Object Name,Length | Format-Table -AutoSize`
>
> </details>
> </details>

已完成组员B的全部核心任务。以下是完整的工作总结。

## 组员B 任务完成报告

### 一、计划执行情况

| 步骤    | 内容                                                         | 状态 |
| ------- | ------------------------------------------------------------ | ---- |
| 1. 调研 | 摸清 Workspace Tool / Skill / Functions 三种扩展的机制与差异 | ✅    |
| 2. 设计 | 选定「章节随机抽题 + 客观题自动判分」，定义结构化 JSON 输入输出 | ✅    |
| 3. 实现 | 编写工具源码（32 题题库 + 两个工具函数）                     | ✅    |
| 4. 测试 | 18 个单元测试 + 真实运行环境加载验证                         | ✅    |
| 5. 文档 | 元数据登记、使用说明、测试样例、工作记录                     | ✅    |
| 6. 提交 | 创建 `codex/tool` 分支并提交                                 | ✅    |

### 二、调研关键结论

本版本（Open WebUI v0.11.0）的扩展工具应使用 **Workspace Tool**：单文件 Python 源码 + `class Tools`，类的公共方法即工具函数，参数通过类型注解/docstring 自动生成 OpenAI function spec，返回 JSON 字符串给模型。这满足题目「真实结构化输入输出、不能只靠提示词模拟」的要求（旧 Functions 已被 Tools 取代，Skill 偏向提示性指导不适用）。

### 三、交付物（`docs/tools/`）

| 文件                   | 说明                                                         |
| ---------------------- | ------------------------------------------------------------ |
| `ds_quiz_tool.py`      | 工具源码：`ds_random_quiz`（章节抽题）+ `ds_grade_quiz`（自动判分），内嵌 32 题题库，覆盖 8 章 |
| `test_ds_quiz_tool.py` | 18 个单元测试（章节解析/抽题过滤/判分/异常/归一化）          |
| `tool-catalog.md`      | 工具元数据 + 输入输出 schema（对齐接口约定 7.1）             |
| `tool-usage.md`        | 接入步骤（UI/API）、自然语言触发示例、测试样例               |
| `worklog-member-b.md`  | 调研结论与验证证据归档                                       |

### 四、验证证据

1. **单元测试**：`python docs/tools/test_ds_quiz_tool.py` → `Ran 18 tests ... OK`
2. **真实环境加载**：用后端实际运行的 conda 环境（Python 3.11.16）调用 `load_tool_module_by_id` 成功加载工具，`get_tool_specs` 生成 2 个函数 spec 正确，`ds_random_quiz`/`ds_grade_quiz` 实际执行返回正常（判分 score=100）
3. **返回结构**统一为 `{"ok": true, "data": ...}` / `{"ok": false, "error": {"code","message"}}`，符合 `interface-agreement.md` 第 7 节

### 五、Git 提交

当前分支 `codex/tool`，两次提交：
- `16d56e4ff feat(tool): 新增数据结构抽题与判分扩展工具`
- `25b170b9f docs(tool): 补充组员B扩展工具工作记录`

### 六、需要组长/集成负责人配合的收尾

「挂到助教 + DeepSeek 端到端对话调用」需要管理员凭据，已提供完整接入步骤（`tool-usage.md` 的方式 A/B）：在 **Workspace → Tools** 粘贴 `ds_quiz_tool.py` 创建工具（ID `ds_quiz`），再在「数据结构 AI 助教」模型的 Tools 字段挂接即可。

**说明**：工作区里还有两个与本次任务无关的未提交状态（`backend/requirements-core.txt` 未跟踪、`backend/open_webui/static/` 下若干文件被删除标记），应是其他成员或部署操作产生的，我没有改动它们。

> 我现在运行打开网页，在前端页面哪里可以看到你实现的扩展功能

<details><summary>2 previous messages</summary>

> 我先确认一下当前的实际状态（工具是否已创建、前端路由），再给你准确的路径。
>
> <details><summary>Ran 3 commands</summary>
>
> - Ran `& 'C:\Users\12461\miniconda3\python.exe' -c @"
> import sqlite3
> conn = sqlite3.connect(r'backend/data/webui.db')
> conn.row_factory = sqlite3.Row
> cur = conn.cursor()
> print('--- tool 表 ---')
> for r in cur.execute('SELECT id, name FROM tool'):
>     print(dict(r))
> print('--- model 表 ---')
> for r in cur.execute('SELECT id, name, base_model_id FROM model'):
>     print(dict(r))
> print('--- 以上若为空则说明工具/模型尚未创建到前端 ---')
> conn.close()
> "@`
> - Ran `Get-ChildItem -Path 'src\routes\workspace' -ErrorAction SilentlyContinue | Select-Object Name; Write-Output '=== 或 src/routes/(app) ==='; Get-ChildItem -Path 'src\routes' -Recurse -Directory -Depth 2 -ErrorAction SilentlyContinue | Where-Object { $_.Name -match 'tool|workspace|model' } | Select-Object FullName | Format-Table -AutoSize -Wrap`
> - Ran `Get-ChildItem -Path 'src\routes\(app)\workspace' -Directory | Select-Object Name | Format-Table -AutoSize; Write-Output '=== workspace 页面布局标题 ==='; Get-ChildItem -Path 'src\routes\(app)\workspace' -File | Select-Object Name`
>
> </details>
> </details>

先给结论：**当前在前端还看不到**——因为工具目前只是一份源码文件（`docs/tools/ds_quiz_tool.py`），还没有通过界面/API 创建进 Open WebUI（我查了后端数据库，`tool` 表和 `model` 表目前都是空的）。

下面是完整的查看/使用路径。

## 1. 先登录

打开前端 http://localhost:5173 ，用管理员账号（`hhd@qq.com`）登录。

## 2. 创建工具（把代码变成前端可见的工具）

1. 进入 **Workspace（工作区）** 页面，URL：`http://localhost:5173/workspace`
2. 在 Workspace 里点 **Tools（工具）** 标签，URL：`http://localhost:5173/workspace/tools`
3. 点「创建新工具」按钮，会打开一个代码编辑器（带官方模板）
4. 把 `docs/tools/ds_quiz_tool.py` 的**全部内容**粘贴进去（覆盖模板）
5. 工具名填「数据结构练习题」，工具 ID 手动改成 `ds_quiz`（纯英文小写）
6. 点 **Save & Create** 保存

保存成功后，**Workspace → Tools 列表里就会出现「数据结构练习题」这个工具**，点进去可以查看代码、描述和它暴露的两个函数（`ds_random_quiz` 抽题、`ds_grade_quiz` 判分）——这就是你实现的功能在前端的呈现位置。

## 3. 真正"使用"（挂到助教模型 + 对话触发）

工具创建后只是存在，要在对话里生效还需要两步：

1. **挂载**：进入 **Workspace → Models**（`http://localhost:5173/workspace/models`）→ 打开「数据结构 AI 助教」→ 在 **Tools** 字段里勾选 `ds_quiz` → 保存。
2. **触发**：回到 **Chat** 对话页，选「数据结构 AI 助教」，发类似：
   - “从第 3 章随机抽 3 道选择题，先不要给答案”
   - “判分，我的答案是 B、B、对”

这时模型会自动调用工具，你就能看到工具返回的题目/判分结果。

---

**一句话总结**：工具代码我已完成并提交在 `codex/tool` 分支的 `docs/tools/` 下，但**前端要看到它，需要先在 `Workspace → Tools` 里粘贴代码创建**；创建后工具出现在 Tools 列表，挂到助教模型后在 Chat 对话中才能被调用。如果你希望我直接帮你把工具通过后端 API 创建进 Open WebUI，需要管理员登录后的 token（当前我拿不到，被 401 拦住了）。

> 现在请将修改上传到远程仓库的main中

<details><summary>6 previous messages</summary>

> 我先检查远程仓库配置和当前分支状态，再执行合并与推送。
>
> <details><summary>Ran 2 commands</summary>
>
> - Ran `git remote -v 2>&1; Write-Output '=== branches ==='; git branch -a 2>&1; Write-Output '=== main vs codex/tool ==='; git log --oneline main..codex/tool 2>&1; Write-Output '=== current branch ==='; git branch --show-current 2>&1`
> - Ran `Write-Output '=== local main vs origin/main ==='; git log --oneline origin/main..main 2>&1; Write-Output '=== origin/main vs local main ==='; git log --oneline main..origin/main 2>&1; Write-Output '=== local main HEAD ==='; git log --oneline -3 main 2>&1; Write-Output '=== origin/main HEAD ==='; git log --oneline -3 origin/main 2>&1`
>
> </details>
>
> 远程与本地 main 完全同步，`codex/tool` 领先两个提交。现在把这两个提交合入 main 并推送。先合并到本地 main。
>
> <details><summary>Ran a command</summary>
>
> - Ran `git checkout main && git merge codex/tool && git log --oneline -5`
>
> </details>
>
> 合并成功（fast-forward）。现在推送到远程 main。
>
> <details><summary>Ran 2 commands</summary>
>
> - Ran `git push origin main 2>&1`
> - Ran `git status -sb 2>&1; Write-Output '=== main vs origin/main ==='; git log --oneline -3 main 2>&1; Write-Output '=== 确认同步 ==='; git rev-parse main origin/main 2>&1`
>
> </details>
> </details>

已成功上传到远程仓库的 main 分支。

## 完成情况

| 操作 | 结果                                                    |
| ---- | ------------------------------------------------------- |
| 合并 | `codex/tool` → `main`（fast-forward）                   |
| 推送 | `a51ce2f3f..25b170b9f main -> main`                     |
| 同步 | 本地 main 与 `origin/main` 均指向 `25b170b9f`，完全一致 |

## 已上传到远程 main 的内容

远程仓库 `https://github.com/boredboring/open-webui-ai.git` 的 main 分支现在包含以下两个提交（5 个文件）：

1. `16d56e4ff` — `feat(tool): 新增数据结构抽题与判分扩展工具`
   - `docs/tools/ds_quiz_tool.py`（工具源码，抽题 + 判分）
   - `docs/tools/test_ds_quiz_tool.py`（18 个单元测试）
   - `docs/tools/tool-catalog.md`（工具元数据）
   - `docs/tools/tool-usage.md`（使用说明）
2. `25b170b9f` — `docs(tool): 补充组员B扩展工具工作记录`
   - `docs/tools/worklog-member-b.md`

当前分支为 `main`，与远程同步（`## main...origin/main`，无 ahead/behind）。

> 提示：工作区里还有一个与本任务无关的未跟踪文件 `backend/requirements-core.txt`（他人/部署产生的），我没有改动或提交它。